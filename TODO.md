# 云奕学项目待办事项

> 最近更新：2026-08-01（Asia/Shanghai）

本文件是仓库级未完成事项的唯一账本。它记录已经确认范围内的后续工作、门禁、阻塞和验证证据，但不构成实施授权，也不得覆盖项目所有者的最新指令或 `AGENTS.md`。

## 状态定义

- `待确认`：必须由项目所有者作出决定或授权；
- `待处理`：范围已确认，前置条件满足后可以实施；
- `进行中`：当前任务正在实施；
- `阻塞`：存在明确的外部依赖或前置条件；
- `已完成`：实现及相称验证均已完成，并记录证据；
- `已取消`：项目所有者取消、范围移除或被其他事项替代，保留原因。

## 当前基线事实

- 第四里程碑 4A 已通过 PR #2 合并到 `main@953d2d2`。
- PR 头提交 `b88e765` 的 GitHub Actions 运行 `30420051453` 全部成功。
- 合并后 `main` 的 GitHub Actions 运行 `30420229605` 共 16 项作业全部成功。
- 第五里程碑实现分支 `codex/milestone-5` 从 `main@953d2d2` 建立，并已接入设计提交 `39578a6`。
- 项目所有者于 2026-08-01 明确授权 4B，并确认 WSL2、个人版 Docker Desktop、
  C++ GCC 15.2.0、Python 3.14.3、Runner `1.1.0`、资源限制、安全边界、失败清理及
  受限 `verified`／`retained` 证据规则。
- 4B 安装前检查确认内存 31.46 GB、C 盘可用 101.85 GB、D 盘 NTFS 可用
  136.14 GB；Docker/WSL 数据根目录使用 `D:\CloudStudy\DockerData`。固件虚拟化已由
  项目所有者启用；任务管理器显示“虚拟化：已启用”，Windows 只读复核为
  `HypervisorPresent=True`、VBS 正在运行且已检测到 Hyper-V 管理程序。此前
  `Win32_Processor` 三项能力字段为 `False` 是管理程序接管后的不可靠信号。
- 项目所有者于 2026-08-01 根据精确镜像与 VHDX 的实测占用，将 Docker 数据根目录
  硬预算从 5 GB 明确调整为 6 GB；启动安装前至少 8 GB 可用的门槛不变。
- 4B 代码纵向切片已在 `codex/milestone-4b` 完成；只读容器数据通道修复后，
  Node 24.14.0 下最新完整本地 `release-readiness` 成功，包括 75 项后端测试、
  15 项前端测试、生产构建、桌面／移动 E2E、契约无漂移和密钥扫描。真实 Runner
  十项矩阵另行全部通过；两者都不能自动解除 5C 实验门禁。
- 4B 实现提交 `34baf87` 已推送到 `codex/milestone-4b` 并创建 Draft PR #6；
  该提交的 GitHub Actions 运行 `30695345061` 共 21 项作业全部成功。PR 尚未合并。
- 项目所有者于 2026-07-29 授权实施 5B，并确认 5B 只调用
  `deepseek-v4-flash`；市场范围采用首版可变版本，来源只允许权威机构或数据所有方的一手官方数据。
- 5B 费用门禁为每月 ¥5、每日 ¥0.50、单次研究 ¥0.20；达到月度 50% 和 80% 告警，
  达到任一硬上限立即停止，禁止自动充值和静默切换模型。
- 用户目标由用户在运行时选择，允许考试、纯学习和其他非变现目标；系统不得默认进入求职或其他变现路径。
- 5A 实现提交 `826d732` 已通过 PR #3 合并到 `main@2f847c6`；PR 头提交
  `f92cab1` 的 GitHub Actions 运行 `30434488897` 共 19 项作业全部成功，合并后
  `main` 的运行 `30523076260` 共 19 项作业全部成功。
- 5B 代码纵向切片、审查修复、深度审计阻断修复及 `FIX-5B-006` 至
  `FIX-5B-010` 重要修复已在 `codex/milestone-5b` 工作区完成，并重新通过完整本地
  `release-readiness`；来源访问采用成功 7 天、失败 24 小时且首版无人工绕过的受管
  规则。2026-07-30 已真实访问国家统计局、工信部和 DeepSeek 官方价格页，并真实调用一次
  `deepseek-v4-flash`；内容结构校验失败，保守记账 ¥0.003597，未生成可采纳结论。工作区
  最终头提交 `0dfef1c` 已通过 PR #4 合并到 `main@273c190`；自动 PR CI
  `30523980823` 和合并后主线 CI `30524441148` 均为 19 项作业全部成功。

## 当前门禁

| 编号 | 状态 | 事项 | 前置条件或阻塞 | 下一步 |
| --- | --- | --- | --- | --- |
| M4A-001 | 已完成 | 等待第三里程碑 Draft PR 合并 | PR #1 已于 2026-07-28 合并为 `a47d3ba` | 已从更新后的 `origin/main` 建立 `codex/milestone-4` |
| M4A-002 | 已完成 | 获得第四里程碑 4A 代码实施授权 | 项目所有者已于 2026-07-28 明确授权 | 严格实施 4A；4B 仍需单独确认 |
| M4B-001 | 已完成 | 单独确认第四里程碑 4B 隔离代码运行 | 项目所有者于 2026-08-01 确认推荐基线并允许安装 WSL2 与个人版 Docker Desktop、接受其许可 | 严格按确认基线实施 |
| M4B-002 | 已完成 | 实现 4B 最小完整纵向切片 | Runner `1.1.0`、`algorithm@0.2.1`、迁移、API、界面、安全治理和自动化测试形成闭环 | Node 24.14.0 下完整本地 `release-readiness` 成功：74 项后端、15 项前端、生产构建、桌面／移动 E2E、契约无漂移和密钥扫描 |
| M4B-003 | 已完成 | 安装并真实验证 WSL2/Docker 隔离运行 | WSL 2.7.11、Docker Desktop 4.84.0 和 Engine 29.6.2 已安装；GCC 15.2.0、Python 3.14.3 按摘要核验；数据根 5.256 GB 未超过 6 GB；`pnpm check:runner-live` 十项全部成功 | 保持练习时 `--pull never`，运行时更新必须发布新版本 |

## 第四里程碑 4B

4B 只在新的 `algorithm@0.2.1` 草稿中启用双语言确定性代码练习；
`algorithm@0.2.0` 不原地修改、不自动迁移，既有历史和进行中记录继续可读。

| 编号 | 状态 | 事项 | 完成条件 |
| --- | --- | --- | --- |
| M4B-004 | 已完成 | 建立 Runner `1.1.0` 请求、结果和运行时契约 | 历史 `1.0.0` 保留；严格 Schema、稳定失败码、摘要、资源和精确镜像记录通过 `check:runner-contract` |
| M4B-005 | 已完成 | 建立 Docker 隔离执行器 | 静态安全参数、无宿主挂载／Docker Socket、并发 1、输出实时截断和失败清理已有单元测试并通过 `check:runner-security` |
| M4B-006 | 已完成 | 发布 `algorithm@0.2.1` 草稿 | `0.2.0` 未原地修改且入口关闭；新版本锁定 GCC/Python 精确摘要和三项受管 Runner 任务，注册表与 manifest 治理通过 |
| M4B-007 | 已完成 | 增加运行记录、API 和界面 | Alembic `0009`、追加式 invocation、逐测试结果、明确执行按钮、失败纠错及不可信输出文本展示已实现并通过服务、前端与 E2E 测试 |
| M4B-008 | 已完成 | 实施受限 Runner 证据 | 服务测试证明精确任务通过才产生对应 `verified`，延迟同范围 Runner 复测才产生 `retained`；失败和基础设施错误不产生证据，且不生成整体掌握或 5C 解锁 |
| M4B-009 | 已完成 | 完成真实安全和失败清理验证 | `pnpm check:runner-live` 真实通过 C++／Python、断网、只读根、无仓库挂载、无 Docker Socket、进程、3 秒超时、64 KiB 合并输出和 256 MB 内存十项探针；每项后无项目标签容器残留 |
| M4B-010 | 已完成 | 完成 4B 远程 CI | 实现提交 `34baf87` 的 Draft PR #6 GitHub Actions 运行 `30695345061` 共 21 项作业全部成功；PR 保持 Draft 且未合并 |

## 第四里程碑 4A

实施范围严格限于 `algorithm@0.2.0` 共同主干入口这一条完整纵向切片；保留 `algorithm@0.1.0` 及其历史引用。

| 编号 | 状态 | 事项 | 完成条件 |
| --- | --- | --- | --- |
| M4A-003 | 已完成 | 研究并确定复习策略 | `docs/research/review-policy.md` 记录来源、限制和确认结果；4A 使用第 1、2、4、7、15 天固定递增日程，失败后纠错并于次日重测 |
| M4A-004 | 已完成 | 扩展技能包 Schema 和契约 | 新增学习、评估、量表、复习和掌握范围 Schema；`check:contracts`、契约无漂移通过 |
| M4A-005 | 已完成 | 关闭 `0.1.0` 入口并新增 `algorithm@0.2.0` 草稿 | 注册表默认开放 `0.2.0`；`0.1.0` 关闭入口并由迁移冻结活动诊断和草稿规划；迁移测试通过 |
| M4A-006 | 已完成 | 完成共同主干入口学习内容 | 三个入口单元、综合检查点、双语言代码文本、迁移、纠错、作品和复习模板已受治理；`check:learning-content` 通过 |
| M4A-007 | 已完成 | 实现确定性诊断补救映射 | 结构化单选信号控制编程与数学补救，规划和执行界面展示原因；桌面/移动 E2E 验证 |
| M4A-008 | 已完成 | 新增学习执行数据模型与迁移 | 迁移 `0005`、内容锁、执行锁、活动、attempt、评估、证据、复习和审计已实现；升级、降级和冻结测试通过 |
| M4A-009 | 已完成 | 实现学习执行状态机 | 非终态唯一、`retention_pending`、失败纠错重测、终态不可恢复与确认复用均有服务测试 |
| M4A-010 | 已完成 | 实现来源变化受限流程 | 只保留确定性元数据差异及独立 `source_review_pending`；接受提示明确不生成影响报告、补充单元或迁移 |
| M4A-011 | 已完成 | 实现学习、练习、纠错、证据和复习 API | 历史规划确认、今日任务、追加修正、六维证据、固定复习和终态接口已写入 OpenAPI；无代码执行、文件或外部 AI 入口 |
| M4A-012 | 已完成 | 实现共同主干入口学习界面 | 历史规划、今日任务、原因、修正、证据、复习、终态复用及限制提示已实现；桌面/移动 E2E 通过 |
| M4A-013 | 已完成 | 增加确定性治理检查 | `check:learning-content`、`check:assessment`、`check:review-policy`、`check:mastery-policy` 已纳入本地、CI 和发布就绪入口 |
| M4A-014 | 已完成 | 完成自动化验证 | Node 24.14.0 下 2026-07-29 完整 `release-readiness` 通过：21 后端、5 前端、桌面/移动 E2E、构建、契约和敏感信息检查成功 |
| M4A-015 | 已完成 | 完成发布就绪验证与远程审查 | 本地 `release-readiness` 通过；提交 `b9adae0` 已推送，GitHub Actions 运行 `30364486210` 全部成功 |
| M4A-016 | 已完成 | 修复 4A PR 审查阻断问题 | 恢复 `algorithm@0.1.0` 不可变 manifest；自评不通过可重新打开追加式纠错；量表严格绑定活动；Node 24.14.0 下完整 `release-readiness` 通过 |
| M4A-017 | 已完成 | 完成 4A PR 审查与合并 | PR #2 已合并为 `953d2d2`；PR CI `30420051453` 和 `main` CI `30420229605` 全部成功 | 4A 已进入主线，继续保持 4B 独立门禁 |
| M4A-018 | 已完成 | 修复 CI 的 uv 工具版本漂移 | 共享 action 已固定 `uv 0.11.32`；PR 和合并后主线 CI 均成功 | 保持工具版本与 `apps/api/pyproject.toml` 约束一致 |

## 第五里程碑：证据约束的变现准备与持续更新

详细范围与完成标准见
`docs/architecture/monetization-and-continuous-update.md`。本节状态只表示范围、门禁和工作准备情况，
不单独构成代码实施、外部调用、付费资源或对外动作授权。

| 编号 | 状态 | 事项 | 前置条件或完成标准 |
| --- | --- | --- | --- |
| M5-001 | 已完成 | 确认第五里程碑阶段边界 | 初次授权只包含无外部调用的 5A；项目所有者之后单独授权 5B，5C 仍不在范围；真实算法交付实验等待 4B 或其他独立验证证据 |
| M5-002 | 已完成 | 从已验证的最新主线建立实现分支 | `codex/milestone-5` 基于 `main@953d2d2` 并接入设计提交 `39578a6`；主线 CI `30420229605` 成功 |
| M5-D01 | 已完成 | 落档第五里程碑详细可执行目标 | `docs/architecture/monetization-and-continuous-update.md` 已明确 5A 交付物、阶段门禁、数据/API/UI 边界、测试矩阵、完成标准和待确认决定；Markdown、结构、API 与差异检查通过 |
| M5A-000 | 已完成 | 实施第五里程碑 5A | 本地实现与完整 `release-readiness` 已通过；PR #3 已合并到 `main@2f847c6`，PR CI `30434488897` 和合并后主线 CI `30523076260` 均为 19 项成功；5B 后续已获独立授权，5C 仍不在范围 |
| M5A-001 | 已完成 | 建立版本化目标选择、准备度、市场证据和路径比较契约 | 五个 JSON Schema、稳定原因码及 OpenAPI/TypeScript 契约已实现；正反例与跨字段测试、契约漂移检查通过 |
| M5A-002 | 已完成 | 实现本地确定性证据聚合与准备度门禁 | 读取 4A 六维证据、复核标记和精确技能版本；非变现目标返回 `not_applicable`，真实用户由代码及数据库双重禁止 `experiment_ready` |
| M5A-003 | 已完成 | 建立合成市场快照和确定性三路径比较 | 四类版本化市场夹具与非变现/证据不足场景覆盖当前、过期、冲突、无法判断和允许比较；全部显著标记为合成 |
| M5A-004 | 已完成 | 建立不可变快照、评估、比较与审计存储 | Alembic `0006`、SQLAlchemy 2 模型、同 ID+版本摘要冲突拒绝、追加式决定与审计已实现；升级、约束和降级测试通过 |
| M5A-005 | 已完成 | 实现本地 API 与桌面/移动界面 | `/readiness` 目标、范围、快照、评估、比较、决定及历史接口和页面已完成；桌面/移动 E2E 验证考试目标不进入三路径比较 |
| M5A-006 | 已完成 | 增加治理检查和自动化验证 | `check:market-sources`、`check:monetization-policy`、`check:external-call-boundary` 已进入本地、CI 和发布门禁；完整本地 `release-readiness` 与远程 CI `30434234513` 成功 |
| M5A-007 | 已完成 | 完成 5A PR 审查、合并与主线验证 | PR #3 头提交 `f92cab1` 无审查阻断并通过 CI `30434488897`；已合并为 `main@2f847c6`，合并后 CI `30523076260` 共 19 项成功 |
| M5B-001 | 已完成 | 接入真实市场研究与持续更新代码纵向切片 | 官方来源、DeepSeek、隐私、预算、审计和用户复核闭环已实现；完整本地 `release-readiness` 成功 |
| M5B-002 | 已完成 | 建立受管官方来源与持续更新状态机 | 四个官方来源、HTTPS/主机/robots/大小门禁、哈希差异、周检、不可访问非无变化、元数据-only 与确认摘录删除均有测试 |
| M5B-003 | 已完成 | 建立 DeepSeek 精确模型与成本门禁 | 只允许官方 `deepseek-v4-flash`；价格页复核、月/日/单次上限、30 天一次综合、无重试/无切换及超时最坏预算计入均有测试 |
| M5B-004 | 已完成 | 建立外发隐私、人工复核与内容隔离 | Windows 凭据引用、外发总开关、逐次确认、最少净化材料、结构化输出和人工接受/拒绝已实现；不会自动修改 5A、技能包或计划 |
| M5B-005 | 已完成 | 完成 API、契约和桌面/移动界面 | Alembic `0007`、OpenAPI/TypeScript、`/market-research` 页面和空/阻断状态已实现；桌面与移动 E2E 通过 |
| M5B-006 | 已完成 | 完成本地发布就绪验证 | `release-readiness` 成功：36 项后端、9 项前端、生产构建、桌面/移动 E2E、契约无漂移与密钥扫描通过；验证费用 ¥0 |
| M5B-007 | 已完成 | 修复 5B 审查发现的费用与证据完整性问题 | 原子付费调用、调用前 token 上限、供应商适配器、有效来源引用、撤回失效、目录与预算版本锁、价格精确核验、审计历史和安全测试已实现；Node 24.14.0 下完整 `release-readiness` 通过：44 项后端、10 项前端、桌面/移动 E2E、契约无漂移与密钥扫描，费用 ¥0 |
| M5B-AUDIT-001 | 已完成 | 处理 5B 深度审计发现的阻断级问题并校正交付状态 | `FIX-5B-001` 至 `FIX-5B-004` 已实现；Node 24.14.0 下完整离线 `release-readiness` 成功：53 项后端、12 项前端、生产构建、桌面/移动 E2E、契约无漂移和密钥扫描，费用 ¥0 |
| M5B-AUDIT-002 | 已完成 | 修复完整来源变化检测 | 原始响应、完整规范化正文和短摘录哈希用途已分离；测试证明摘录外正文变化为 `changed`，仅模板变化为 `metadata_changed`，真实外部访问为 0 |
| M5B-AUDIT-003 | 已完成 | 修复三路径证据能力与独立性门禁 | 路径能力、`independence_group` 和本次内容相关性已进入 Schema、治理、服务测试和 UI；当前三路径均诚实声明 `context_only` 并固定不可确定判断 |
| M5B-AUDIT-004 | 已完成 | 移除通用市场研究流程中的算法硬编码 | 版本化注册表及技能、精确版本、能力范围、当前目标、可选准备度评估锁已实现；第二个测试技能无需修改核心服务即可创建受管研究 |
| M5B-AUDIT-005 | 已完成 | 增加付费调用租约、崩溃恢复与保守记账 | 持久化 attempt、阶段审计、5 分钟租约、启动恢复、`recovery_required`、未知收费最坏记账及显式结束接口均通过并发、异常与恢复测试 |
| M5B-AUDIT-006 | 已完成 | 修复 Windows 固定 pytest 临时目录阻断 | 本地与 CI 统一入口改用仓库内每次唯一的 pytest `basetemp`；53 项后端测试及完整 `release-readiness` 成功 |
| M5B-IMP-001 | 已完成 | 修复失败或阻断后的官方来源重复访问 | 来源目录 `1.2.0` 固定成功 7 天、失败 24 小时且无人工绕过；逐来源访问/成功快照、部分失败定向重试、API/UI 倒计时和审计已实现，57 项后端、13 项前端及桌面/移动 E2E 等完整门禁通过 |
| M5B-IMP-002 | 已完成 | 增加外发材料知情预览与摘录删除闭环 | 后端预览与实际请求复用净化材料；UI 展示发送/排除类别并逐项删除；桌面 E2E 验证接受、删除和综合失效 |
| M5B-IMP-003 | 已完成 | 修复后端失败后的前端旧状态 | `ApiError` 后保留原错误并刷新 overview/history；移动 E2E 验证模型失败后无需手动刷新即可显示终态、失败码和保守记账 |
| M5B-IMP-004 | 已完成 | 校验并审计 DeepSeek 响应模型标识 | Alembic `0008` 分别保存请求与响应模型；缺失、不一致和精确匹配测试通过，不一致按单次 ¥0.20 上限保守记账 |
| M5B-IMP-005 | 已完成 | 强化 5B 治理与行为保证 | 治理检查增加所有者独立组、路径能力/允许路径和刷新策略一致性；关键保证由服务反例测试覆盖 |
| M5B-IMP-006 | 已完成 | 扩展 5B 完整离线状态机 E2E | 独立离线 FastAPI 宿主覆盖桌面成功/删除失效和移动模型失败/状态刷新；外部访问与付费调用均为 0 |
| M5B-LIVE-001 | 阻塞 | 执行首次真实供应商烟雾验证 | 2026-07-30 已真实调用一次 `deepseek-v4-flash`：只发送国家统计局和工信部两条净化短摘录，官方价格核验通过，HTTP 200 且响应模型标识精确匹配；综合内容未通过 JSON／业务结构校验，运行以 `deepseek_invalid_response` 终止，未生成可采纳结论、未重试或切换模型。实际费用字段为 0，本地按响应使用量保守记账 ¥0.003597，外发总开关已关闭。等待项目所有者决定是否停止，或先实施隐私安全的分阶段响应诊断与更稳健的受限输出协议；任何再次调用仍需新的逐次费用确认 |
| FIX-5B-011 | 已完成 | 修复 DeepSeek 官方价格表解析与预派发失败恢复 | 已改为按价格标签和模型相对位置解析 `rowspan` 分组表；恢复只接受精确失败码并强制校验无 attempt、dispatch、响应、外发同意、token、费用或计费时间，保留原失败事件且与付费确认分离。真实官方价格页 HTTP 200 且 `pricing_matches=True`；针对性后端 29 项、完整后端 58 项及契约无漂移通过 |
| FIX-5B-012 | 已完成 | 隔离 E2E 与正在运行的 Next 开发服务构建目录 | E2E 使用固定安全目录 `apps/web/.next-e2e`，启动前与停止后校验目标并清理，`tsconfig` 预声明稳定类型路径；启动器还保存并恢复 Next 自动改写的 `next-env.d.ts`。桌面和移动 E2E 通过，运行前后文件 SHA-256 一致 |
| FIX-5B-013 | 已完成 | 增加 DeepSeek 无效响应的隐私安全诊断和受限输出协议 | `limited_background_v1` 只接收背景摘要、来源引用、不确定性与限制；后端固定 `indeterminate / no_change`。无效响应只审计阶段、受管结构摘要、类别、哈希及已知 usage/费用，不保存原文或未知键值。完整 `release-readiness` 通过：60 项后端、14 项前端、生产构建、桌面/移动 E2E、契约无漂移和密钥扫描；新增费用 ¥0，未再次调用模型 |
| FIX-5B-014 | 已完成 | 修复干净 Linux CI 缺少 pytest 临时目录父级 | `tools/run_pytest.py` 显式创建父目录，干净检出回归测试通过；完整本地 `release-readiness` 通过 61 项后端、14 项前端、生产构建、桌面/移动 E2E、契约无漂移和密钥扫描。提交 `638d9c4` 已推送，PR #4 运行 `30520982962` 共 19 项全部成功 |
| M5B-008 | 已完成 | 在 5A 合并后重新基线化并交付 5B | `main@2f847c6` 已合入 `codex/milestone-5b`；Node 24.14.0 下全部本地门禁通过，PR #4 头提交 `0dfef1c` 的自动 CI `30523980823` 共 19 项成功；PR #4 已合并为 `main@273c190`，合并后 CI `30524441148` 共 19 项成功 |
| M5C-001 | 待确认 | 确认非交付型最小实验和学习回流范围 | 本次明确不实施 5C；首个重点路径、最低证据、外部动作、结果与收入记录仍需后续确认 |
| M5C-002 | 阻塞 | 开启真实算法交付类实验 | 等待 4B 或项目所有者确认的其他独立验证证据；4A 自评、文本和结构校验不能解除门禁 |
| AI-001 | 已完成 | 确认 5B 首批调用模型 | 5B 只允许 DeepSeek 官方 API 的 `deepseek-v4-flash`；现有多供应商配置边界保留，但 5B 不调用 OpenAI 或 Moonshot，不静默切换模型 |
| MARKET-001 | 已完成 | 确认目标市场范围 | 首版范围为中国大陆、中文为主、初级 C++ 后端与算法应用就业、中国大陆中小客户接单、中文个人开发者与算法学习者产品受众；后续调整发布新版本 |
| CONTENT-001 | 已完成 | 确认来源获取与更新方案 | 官方来源目录、主机白名单、HTTPS、robots、短摘录、哈希差异、失败、删除和复核门禁均已固化并通过治理检查 |
| BUDGET-001 | 已完成 | 确认 5B 费用预算与停止条件 | 月 ¥5、日 ¥0.50、单次 ¥0.20；月度 50%/80% 告警，100% 及任一硬上限停止；禁止自动充值，价格变化或无法估价时停止 |
| EVIDENCE-001 | 待确认 | 确认各路径最低证据和独立验证形式 | 不使用单一总分；明确保持、作品、操作、人工或其他独立证据要求 |
| GOAL-001 | 已完成 | 确认目标选择边界 | 目标由用户运行时选择；支持考试、纯学习和其他非变现目标；非变现目标不进入强制市场比较 |

## 运行与来源待办

| 编号 | 状态 | 事项 | 前置条件或完成标准 |
| --- | --- | --- | --- |
| OPS-001 | 待确认 | 提供普通 Windows PowerShell 的稳定启动入口 | 在系统级 Node 24/Corepack/pnpm 与仓库启动器之间确认；必须校验仓库目录 |
| OPS-002 | 已完成 | 修复 Codex Windows 后台服务启动挂起并固化规避规则 | 新增 `local-services:start/status/stop`、实际 Python PID 辅助入口和 45 秒有界健康检查；普通用户 PowerShell 启动已支持本地交互。实测 Codex 普通沙箱与沙箱外隐藏进程都不能跨作业持久存活，`AGENTS.md` 10.4 已明确禁止 Codex 承担长期服务宿主 |
| OPS-003 | 待处理 | 保持 `check:web` 后受跟踪的 `next-env.d.ts` 不漂移 | 2026-07-30 重新验证发现 Next 生产构建会把开发类型路径改为生产类型路径；本次已恢复验证前内容。后续需让统一入口保存并恢复原始字节，并验证成功、失败和退出路径 |
| SOURCE-001 | 待处理 | 复核真实官方来源失败 | 2026-07-30 新鲜证据：国家统计局与工信部 HTTP 200；人社部统计入口返回 `empty_content`；中国公共招聘网返回 `external_timeout`。人社部可于 2026-07-31 11:56:56、中国公共招聘网可于 11:57:00（Asia/Shanghai）后重试；成功来源进入 7 天刷新窗口，冷却到期后只能重试两个失败来源 |
| SOURCE-002 | 待处理 | 完成两个研究来源的人工复核 | 保存可复核证据和最近成功时间，不绕过版权或访问条件 |
| SOURCE-003 | 阻塞 | 将来源修复发布到受管技能包 | 不得原地修改已引用的 `algorithm@0.2.0`；需确认是否发布独立补丁版本及变更范围 |

## 后续规划

| 编号 | 状态 | 事项 | 前置条件或完成标准 |
| --- | --- | --- | --- |
| FUTURE-001 | 待确认 | 基于个人复习结果评估并实施自适应遗忘曲线模型 | 4A 完成并积累足够真实数据；另行确认数学模型、目标回忆率、冷启动、校准验证、隐私、回退和新技能包版本，不在 4A 中实现 |

## 已完成的准备工作

| 编号 | 状态 | 事项 | 证据 |
| --- | --- | --- | --- |
| M4A-D01 | 已完成 | 确认 1A 至 8A 的第四里程碑 4A 详细设计决定 | `docs/architecture/learning-execution.md`；`check:markdown` 和 `check:structure` 通过 |
| M4A-D02 | 已完成 | 明确 4A 与 4B 的边界 | 4A 不执行代码；4B 必须单独确认；关键词交叉检查通过 |
| M4A-D03 | 已完成 | 建立实时待办约束和仓库账本 | 根目录 `TODO.md`、`AGENTS.md` 第 10.3 节及 `check:structure` 必需文件检查 |
| M4A-D04 | 已完成 | 消除第四里程碑二次自查歧义 | 已确认 `9A、10A、11B、12A–18A` 并写入设计和约束；`check:markdown`、`check:structure` 和 `git diff --check` 通过 |

## 维护记录

- 2026-08-01：将 4B 实现提交为 `34baf87`，推送到 `codex/milestone-4b` 并创建
  Draft PR #6。GitHub Actions 运行 `30695345061` 共 21 项作业全部成功，包括 Runner
  契约、安全门禁、API、Web、桌面／移动 E2E 和最终 `release-readiness`；PR 未合并。
- 2026-08-01：Runner 实机修复后在 Node 24.14.0 下重新执行完整
  `pnpm release-readiness`，75 项后端、15 项前端、生产构建、桌面／移动 E2E、
  契约无漂移和密钥扫描全部成功。该本地结果不等于已提交、远程 CI 或 5C 授权。
- 2026-08-01：真实 Runner 首次暴露只读容器拒绝 `docker cp` 写入且守护进程复制视图
  无法可靠读取运行时 tmpfs；改为启动受限容器后，用非 root 的 `docker exec -i tar -x`
  流式注入内存归档，并用容器内 `tar -c` 取回编译产物。仅 `/tmp` 在容量限制、
  `nosuid,nodev` 下显式允许执行。修复后 `pnpm check:runner-live` 十项全部成功，
  Docker Engine 29.6.2，D 盘可用 131.79 GB，数据根占用 5.256 GB，且无标签容器残留。
  根因与 Windows 检测、PATH、长拉取及磁盘计量策略已写入 `AGENTS.md` 第 10.6 节。
- 2026-08-01：项目所有者确认将 Docker 数据根目录硬预算从 5 GB 调整为 6 GB；
  当前非稀疏 VHDX 实占 5.137 GB 未超限，解除 M4B-003／M4B-009 的预算阻塞并继续真实
  Runner 验证。安装前至少 8 GB 可用门槛不变，不自动清理镜像或扩大数据根目录。
- 2026-08-01：Docker Desktop 4.84.0／Engine 29.6.2 启动成功。首次镜像配置发现当前
  Codex 进程尚未继承 Docker CLI／凭据助手 PATH，脚本改为仅在自身进程解析个人版
  Docker 目录；重试后 GCC 15.2.0 与 Python 3.14.3 均按锁定摘要下载并通过版本核验。
  首次拉取约 20 分钟并非死锁，但缺少有界阶段提示。Docker `system df -v` 报告两镜像
  唯一内容合计 3.784 GB，NTFS 上非稀疏 `docker_data.vhdx` 实际为 5.137 GB，超过已
  确认 5 GB 数据根硬预算；在项目所有者决定预算或镜像方案前停止真实 Runner 验证。
- 2026-08-01：修复 PowerShell 5 将“WSL 尚未安装”的原生 stderr 提前转成终止错误，
  并将管理员安装日志写入 `D:\CloudStudy\Logs\setup-runner-windows.log`。UAC 管理员
  执行成功安装 WSL 2.7.11，脚本按设计以 `3010` 停止并要求重启；尚未安装 Docker
  Desktop、下载运行时镜像或运行容器。
- 2026-08-01：项目所有者启用硬件虚拟化并提供任务管理器截图；Windows 复核得到
  `HypervisorPresent=True`、VBS 正在运行且已检测到 Hyper-V 管理程序，解除固件门禁。
  修复安装脚本仅依赖 `Win32_Processor.VirtualizationFirmwareEnabled`、会在管理程序
  已接管时误阻断的问题；4B 实机安装转为进行中。
- 2026-08-01：完成 4B 代码纵向切片：新增 Runner `1.1.0` 契约和 Docker 隔离后端、
  `algorithm@0.2.1`、Alembic `0009`、追加式 invocation、明确执行界面及受限
  `verified`／`retained` 证据。Node 24.14.0 下完整 `release-readiness` 成功：74 项
  后端、15 项前端、生产构建、桌面／移动 E2E、契约无漂移和密钥扫描；E2E 同时验证
  `0.2.1` 不会自动迁移到锁定 `0.2.0` 的 5B 目录。真实 Docker 安装与
  `check:runner-live` 仍因固件虚拟化关闭而阻塞；入口已实际返回
  `docker_unavailable`，未下载镜像、未运行容器，也未解除 5C。
- 2026-08-01：项目所有者明确授权 4B 并确认推荐基线；安装前检查确认资源充足但固件
  虚拟化关闭。建立 `codex/milestone-4b`，登记代码实施与真实容器验证的独立状态；
  Docker/WSL 数据根目录确认使用 `D:\CloudStudy\DockerData`，不触碰 D 盘其他目录。
- 2026-07-28：创建仓库级待办账本；登记第四里程碑门禁、4A 实施切片和 4B 独立确认事项。
- 2026-07-28：完成 1A 至 8A 设计落档和实时待办约束；Node 24.14.0 下 Markdown、结构、Python 格式/静态检查及 Web lint/typecheck 通过。
- 2026-07-28：项目所有者确认第四里程碑二次自查选择为 `9A、10A、11B、12A–18A`；同步详细设计、项目约束和实施待办。
- 2026-07-28：二次设计落档验证完成；Node 24.14.0 下 `check:markdown`、`check:structure` 及 `git diff --check` 通过。
- 2026-07-28：第三里程碑 PR #1 已合并，项目所有者授权第四里程碑 4A 代码实施；从 `origin/main@a47d3ba` 建立 `codex/milestone-4`，复习策略研究开始。
- 2026-07-28：确认 4A 使用第 1、2、4、7、15 天固定递增复习日程及失败规则；记录研究依据与限制，并登记后续自适应曲线规划。
- 2026-07-28：完成 4A 共同主干入口纵向切片；Node 24.14.0 下完整 `release-readiness` 通过，提交、推送和远程 CI 留待项目所有者确认。
- 2026-07-28：项目所有者授权发布；提交 `b9adae0` 已推送至 `codex/milestone-4`，分支远程 CI 运行 `30364486210` 全部成功。
- 2026-07-29：项目所有者授权先完成 4A PR 审查合并；修复不可变历史、自评纠错闭环和活动量表绑定三个审查阻断问题，Node 24.14.0 下完整 `release-readiness` 通过，进入 PR 发布阶段。
- 2026-07-29：PR #2 首次 CI 运行 `30419250782` 因 `setup-uv` 自动升级至 `0.12.0` 而系统性失败；项目所有者确认固定 `uv 0.11.32` 的最小修复方案。
- 2026-07-29：固定 `uv 0.11.32` 后 PR CI `30420051453` 全部成功；PR #2 合并为 `main@953d2d2`，合并后 CI `30420229605` 的 16 项作业全部成功。
- 2026-07-29：项目所有者确认第五里程碑阶段边界：无外部调用的 5A 可先行；5B 等待模型、市场范围、来源方案和预算；真实算法交付实验等待 4B 或其他独立验证证据。
- 2026-07-29：完成第五里程碑详细可执行目标落档；Node 24.14.0 下 `check:markdown`、`check:structure`、`check:api`（21 项测试）及 `git diff --check` 通过。
- 2026-07-29：项目所有者确认本次只实施 5A，接受本地辅助、4B 独立门禁、分层来源和来源补丁边界；5B/5C、真实模型和预算继续待定。目标由用户运行时选择，并允许考试、纯学习及其他非变现目标。
- 2026-07-29：完成 5A 本地纵向切片；Node 24.14.0 下完整 `release-readiness` 成功，包括 27 项后端测试、8 项前端测试、生产构建、桌面/移动 E2E、契约漂移、敏感信息及三项 5A 专用门禁。提交、推送、PR 和远程 CI 等待项目所有者决定。
- 2026-07-29：项目所有者授权发布 5A；提交 `826d732` 已推送并创建 Draft PR #3，GitHub Actions 运行 `30434234513` 共 19 项作业全部成功。5A 实现尚未合并到 `main`，5B/5C 继续保持未授权或阻塞。
- 2026-07-29：项目所有者授权 5B 并确认首版市场范围、权威一手官方来源边界、
  `deepseek-v4-flash` 单模型调用和 ¥5/月等成本门禁；从 5A 已验证提交建立
  `codex/milestone-5b`，开始实现，5C 与 4B 仍未授权。
- 2026-07-29：完成 5B 代码纵向切片；修复系统 pytest 临时目录权限导致的本地门禁问题后，
  完整 `release-readiness` 成功，包括 36 项后端测试、9 项前端测试、生产构建、
  桌面/移动 E2E、契约无漂移和密钥扫描。所有验证使用离线替身，费用 ¥0；
  首次真实 DeepSeek 烟雾验证等待本机凭据和逐次确认。
- 2026-07-29：完成 5B 审查修复：增加付费调用原子抢占与预算预留、调用前 token
  上限、DeepSeek 独立适配器、有效来源与路径引用校验、来源撤回失效、目录和预算
  不可变快照、价格表列级核验及可查询审计历史；Node 24.14.0 下完整
  `release-readiness` 再次成功（44 项后端、10 项前端、桌面/移动 E2E），费用 ¥0。
- 2026-07-29：完成只读深度审计并新增根目录 `待修复.md`；登记
  `M5B-AUDIT-001`，发现来源变化可能漏报、三路径证据能力不足、算法硬编码、
  付费调用缺少崩溃恢复等阻断问题。首次真实供应商烟雾验证改为阻塞，等待项目所有者
  确认修复范围、完成实现并重新取得完整离线验证证据。
- 2026-07-29：项目所有者授权解决 `待修复.md` 的阻断级问题；将
  `M5B-AUDIT-001` 调整为进行中，并登记完整变化检测、三路径证据、通用上下文锁和
  付费调用恢复四项实施与验证工作。重要级问题、真实付费调用、4B 和 5C 不在本次范围。
- 2026-07-29：完整发布门禁复核发现固定 `.pytest-tmp` 的历史 ACL 损坏会在测试收集前
  触发 `PermissionError`；独立唯一 `basetemp` 下全套后端测试成功，登记并实施由本地与
  CI 共用的跨平台唯一 pytest 临时目录启动器。
- 2026-07-29：完成 `FIX-5B-001` 至 `FIX-5B-004`：分离三类来源哈希和变化状态，
  增加路径证据能力、内容相关性与独立来源组，引入通用版本化市场研究注册表及精确上下文
  锁，并实现付费 attempt、5 分钟租约、崩溃恢复、保守记账和显式结束。Node 24.14.0
  下完整离线 `release-readiness` 成功，包括 53 项后端、12 项前端、生产构建、
  桌面/移动 E2E、契约无漂移和密钥扫描，费用 ¥0；工作区仍未提交、推送或真实调用。
- 2026-07-29：项目所有者授权继续解决 `待修复.md` 的重要级问题；登记
  `FIX-5B-005` 至 `FIX-5B-010` 对应的访问冷却、知情外发与删除、失败状态刷新、
  响应模型校验、治理行为保证和完整离线 E2E。4B、5C、真实来源访问与付费调用仍不在范围。
- 2026-07-29：完成 `FIX-5B-006` 至 `FIX-5B-010`：外发预览与删除共用最终净化材料，
  后端失败后刷新真实状态，请求/响应模型分离审计，治理结构检查和服务反例测试加强，并新增
  完全离线的桌面成功/删除失效与移动模型失败流程。Node 24.14.0 下完整
  `release-readiness` 成功：55 项后端、13 项前端、生产构建、桌面/移动 E2E、契约无漂移
  和密钥扫描，费用及真实外部访问均为 0。`FIX-5B-005` 的失败冷却时长与人工例外仍待
  项目所有者确认；建议 24 小时且首版不允许绕过。
- 2026-07-29：项目所有者确认 `FIX-5B-005` 采用失败后 24 小时冷却、首版不允许人工
  绕过，成功刷新仍保持 7 天；`M5B-IMP-001` 进入实施，真实来源访问和付费调用仍不在范围。
- 2026-07-29：完成 `FIX-5B-005`：受管目录升级至 `1.2.0`，逐来源分离最近访问与成功
  快照，成功保持 7 天、失败冷却 24 小时且无人工绕过；部分失败只重试到期来源。Node
  24.14.0 下完整离线 `release-readiness` 成功，包括 57 项后端、13 项前端、生产构建、
  桌面/移动 E2E、契约无漂移和密钥扫描，费用及真实外部访问均为 0。
- 2026-07-30：登记 `OPS-002`。本地服务启动诊断确认 Codex PowerShell 环境中的
  `Path`/`PATH` 重复会使 `Start-Process` 在创建环境字典时失败；后续内联后台启动又因
  子进程继承工具标准句柄而长时间不返回。已终止经路径、启动时间和端口重新核对的本次
  残留进程，开始增加有界、可复核的仓库启动入口并固化代理规避规则。
- 2026-07-30：项目所有者确认原本地数据库中的两条 `algorithm@0.2.0` 诊断为可放弃的
  测试数据。原数据库已原样移动到
  `.tmp/database-backups/cloud-study.test-data-20260730-014614.db`，活动数据库重新创建
  并迁移至 `0008`；备份未删除，活动数据库当前没有诊断记录或技能包内容锁。
- 2026-07-30：完成 `OPS-002`。根目录增加本地服务启动、状态和停止入口；API 使用实际
  Python PID 文件，子进程环境去除 `Path`/`PATH` 重复，日志与状态保存在忽略目录，健康
  检查连接显式关闭。Codex 经批准在沙箱外隐藏启动后，API `/health` 与设置页均返回
  200，状态命令 0.19 秒返回，停止与端口回收成功，启动器错误日志为空；`AGENTS.md`
  10.4 已固化后续操作规则。
- 2026-07-30：后续跨轮次验证推翻“Codex 沙箱外隐藏进程可持久宿主服务”的推断：
  API/Web 在当时健康，但 Codex 作业结束后 PID 和端口均被回收。项目所有者改用普通
  PowerShell 启动后完成 DeepSeek 档案保存；`AGENTS.md` 10.4 和 `OPS-002` 证据已校正，
  后续 Codex 不得再次尝试宿主跨轮次长期服务。
- 2026-07-30：执行首次真实官方来源访问，研究
  `82d58da3-9cab-4e6f-ab14-f433c695c4aa` 中国家统计局与工信部成功，人社部统计入口为空，
  中国公共招聘网超时；没有自动重试，费用 ¥0。由于两个就业相关入口不可用且剩余来源
  都是 `background_context`，外发总开关已重新关闭，DeepSeek 综合等待项目所有者决定。
- 2026-07-30：项目所有者选择等待失败来源冷却后重试。外发总开关保持关闭，DeepSeek
  未调用，费用为 ¥0；人社部可于 2026-07-31 11:56:56、中国公共招聘网可于
  11:57:00（Asia/Shanghai）后重试，国家统计局与工信部继续复用既有结果。
- 2026-07-30：项目所有者随后明确授权只向 `deepseek-v4-flash` 发送国家统计局与工信部
  两条净化短摘录，单次最高 ¥0.20，输出必须保持 `indeterminate`／仅背景信息。预检名单
  与模型锁定正确，但官方价格核验因本地解析器未适配价格表分组列而在 dispatch 前停止；
  DeepSeek 未调用，实际与记账费用均为 ¥0，外发总开关已在同一操作中关闭。登记
  `FIX-5B-011`，等待确认修复解析器和只适用于未 dispatch 终态的受控恢复方案。
- 2026-07-30：完成 `FIX-5B-011` 与 `FIX-5B-012`。修复后的解析器只读访问 DeepSeek
  官方价格页取得 HTTP 200 且确认三项价格匹配；零费用预派发恢复保留原失败审计并与模型
  调用确认分离。E2E 改用并清理独立 `.next-e2e`，不再与用户正在运行的 `.next` 冲突。
  完整 `release-readiness` 成功：58 项后端测试、14 项前端测试、生产构建、桌面/移动
  E2E、契约无漂移和密钥扫描均通过。常驻 API 仍需项目所有者在普通 PowerShell 重启后
  才能加载恢复接口；DeepSeek 仍未调用，费用仍为 ¥0。
- 2026-07-30：项目所有者重启服务并再次逐次确认后，显式恢复原零费用预派发失败并真实
  调用 `deepseek-v4-flash`。外发材料仅含国家统计局和工信部净化短摘录；价格核验成功，
  DeepSeek 返回 HTTP 200 且模型标识匹配，但综合内容未通过 JSON／业务结构校验，运行以
  `deepseek_invalid_response` 终止。没有自动重试或模型切换，外发总开关已关闭；本地实际
  费用字段为 0，按响应使用量保守记账 ¥0.003597。登记 `FIX-5B-013`，等待决定是否先增加
  不保存原始响应的分阶段诊断及由后端固定 `indeterminate` 的受限输出协议。
- 2026-07-30：项目所有者选择实施 `FIX-5B-013`，但没有授权再次付费调用。新增
  `limited_background_v1`：模型只返回背景摘要、来源引用、不确定性和限制，后端固定三条
  路径为 `indeterminate`、内容影响为 `no_change`；无效响应审计不保存原文或未知键值，
  只记录阶段、受管结构摘要、校验类别、哈希及已知 token/费用。定向后端测试 31 项通过；
  随后完整 `release-readiness` 成功：60 项后端、14 项前端、生产构建、桌面/移动 E2E、
  契约无漂移和密钥扫描全部通过。验证只使用离线替身，新增费用与真实外部访问均为 0。
- 2026-07-30：提交前发现 Next E2E 会把受跟踪的 `next-env.d.ts` 改写为临时
  `.next-e2e` 类型路径。启动器现按原始字节在正常、失败、信号与退出路径恢复文件；桌面
  和移动 E2E 再次通过，运行前后 SHA-256 均为
  `7AD303E40D4FDDF44F156129E397511953A71481C5CFD86B1862649AAAF240CC`。
- 2026-07-30：提交 `538c3b3` 已推送并创建 Draft PR #4。GitHub Actions 运行
  `30520437794` 中 17 项检查通过，`api-tests` 因干净 Linux 检出没有被忽略的 `.tmp`
  父目录而出现 57 个夹具初始化 `FileNotFoundError`，汇总 `release-readiness` 随之失败；
  业务断言尚未执行到失败点。登记 `FIX-5B-014`，等待项目所有者确认最小修复。
- 2026-07-30：项目所有者确认实施 `FIX-5B-014`。pytest 启动器现先创建仓库 `.tmp`
  父目录，再生成每次运行唯一的 `--basetemp`；回归测试从不存在的模拟仓库根目录开始，
  证明父目录会创建且连续运行路径不复用。定向格式、lint、mypy 与 3 项测试通过；完整
  本地 `release-readiness` 随后成功，包括 61 项后端、14 项前端、生产构建、桌面/移动
  E2E、契约无漂移和密钥扫描。
- 2026-07-30：`FIX-5B-014` 提交 `638d9c4` 已推送。PR #4 的 GitHub Actions 运行
  `30520982962` 共 19 项作业全部成功；Linux `api-tests` 在干净检出中跑完 61 项测试，
  汇总 `release-readiness` 成功。5B 仍未合并到 `main`，修复后也未再次调用付费模型。
- 2026-07-30：完成 PR #3 代码审查，未发现阻断项；PR 头提交 `f92cab1` 的 CI
  `30434488897` 共 19 项成功。PR #3 已转为 Ready 并合并为 `main@2f847c6`，
  合并后 CI `30523076260` 共 19 项成功。随后将该主线合入本地
  `codex/milestone-5b`，开始重新验证 PR #4；未调用 DeepSeek，4B 与 5C 保持未授权。
- 2026-07-30：5B 重新验证中的治理/契约组、61 项后端测试、14 项前端测试与生产构建、
  桌面/移动 E2E、契约无漂移和密钥扫描全部成功。验证后发现 `check:web` 会改写受跟踪的
  `next-env.d.ts`，已恢复验证前内容并登记 `OPS-003`；该清洁度修复不在本次 PR #4
  合并范围内。
- 2026-07-30：完成 PR #4 最终审查，未发现阻断项；PR 头提交 `0dfef1c` 的手动 CI
  `30523905091` 与自动 PR CI `30523980823` 均为 19 项成功。PR #4 已转为 Ready 并
  合并为 `main@273c190`，合并后 CI `30524441148` 共 19 项成功。整个重新验证与合并
  流程未调用 DeepSeek 或访问真实来源；4B、5C 仍未授权，下一次真实模型验证继续受
  30 天间隔和逐次费用确认约束。
