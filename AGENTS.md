# AGENTS.md

本文件是本项目中所有 AI 编码代理必须遵守的项目级约束。它适用于整个仓库。

## 1. 项目使命

构建一个目前仅供项目所有者本人使用的 AI 学习系统，帮助用户：

1. 学习一项技能；
2. 通过测验、实践和真实作品证明掌握程度；
3. 将已掌握的技能转化为就业、自由职业接单或产品化收入的能力；
4. 持续追踪知识变化，并更新学习路径。

本项目不能把“看完内容”等同于掌握技能，也不能把预测的变现机会描述为确定收入。

## 2. 已确认的产品决定

- 初始用户：仅项目所有者本人。
- 初始商业模式：免费验证，不设计付费功能，除非用户之后明确要求。
- 内容模式：AI 从全球范围内尽可能权威的来源检索、整合、校验和动态维护学习内容。
- 变现方向：系统根据技能、掌握证据和市场信息评估就业、自由职业接单、产品化三条路径，并尽可能同时支持。
- 技能扩展方式：先完整做好一个技能；底层采用可复用的通用学习引擎，后续以独立“技能包”的方式逐个增加新技能，不能把首个技能硬编码进通用流程。
- 首个技能：算法。
- 产品名称：云奕学。
- 初版视觉方向：允许 AI 先提出并记录可修改的视觉方向提案，后续可以根据实际使用体验调整。提案不得被描述为不可变的最终品牌规范；Logo、商标、公开品牌资产和大规模视觉改版仍须在实际制作前确认范围。
- 仓库许可证：项目所有者拥有版权的项目代码和文档采用 PolyForm Noncommercial License 1.0.0，版权人为“云奕学项目所有者（joonas-001）”。该许可证只允许规定范围内的非商业使用，不得标注为 MIT License 或 OSI 开源许可证。商业使用必须另行取得版权所有者授权；第三方依赖和引用内容继续适用各自的许可证与条款。
- 算法技能范围：采用综合路线，以数据结构与算法为共同主干，并覆盖工程应用、求职面试、算法竞赛和算法理论。应避免四条路线重复教授相同基础内容。
- 算法路线组织：用户完成并通过共同主干的阶段评估后，再逐步解锁工程应用、求职面试、算法竞赛和算法理论四个分支；不得在基础不足时同时堆叠四条路线。
- 用户当前基础：了解一些 Python 和 C/C++，但基础尚不扎实，学习路径必须先通过诊断确认缺口并补牢必要的编程基础。
- 算法实践语言：以 C++ 为主，同时支持 Python；需要说明同一算法在两种语言中的实现差异、标准库使用方式和性能差异。
- 算法学习目标：系统掌握算法理论，提高实际开发能力，具备求职面试能力，并能够参与算法竞赛。
- 用户学习时间：每天约 2 小时。计划必须能拆分为可执行的每日任务，并根据实际完成情况动态调整，不能假设用户每天都能严格按计划完成。
- 用户数学基础：只学过微积分且掌握不牢。算法路线必须先诊断所需数学基础，并按需补充离散数学、逻辑、组合数学、概率和算法分析所需内容，不能假定用户已经具备这些知识。
- 效果与成本偏好：前期优先保证学习和 AI 规划效果，再评估和控制模型、检索、存储及部署费用；任何可能持续产生费用的方案仍须提前说明计费方式和大致成本，不能擅自开通付费资源。
- 计划制定方式：AI 在生成学习计划前，应根据用户目标和已有回答进行动态、递进式访谈。后续问题可以由 AI 合理规划，不限于固定问卷；当信息足够时停止提问并解释规划依据，避免无意义或重复提问。
- 运行与部署策略：先在用户电脑本地完成首版开发和验证；通过约定的验证标准后，再制定互联网部署方案。未经用户后续确认，不得提前创建线上服务或部署公开网站。
- 首版本地技术栈：前端使用 Next.js，后端使用 FastAPI，本地数据使用 SQLite。互联网部署前可以重新评估数据库和运行架构，但迁移必须保留现有学习、来源和评估数据。
- 前端包管理器：使用 pnpm。不得混用 npm、Yarn 或 Bun，也不得生成其他包管理器的锁文件。
- JavaScript 运行时基线：Node.js 24 LTS。根目录必须声明并锁定该主版本；本地开发和 CI 使用同一主版本。
- Python 运行时基线：Python 3.14.3。不得在未验证兼容性和迁移影响前擅自改变该版本。
- 如果必要的 Python 依赖不支持 Python 3.14，必须停止实施并报告具体依赖、失败证据和可选处理方式；未经用户确认不得降低 Python 版本。
- Python 项目与依赖管理：使用 uv，依赖声明保存在 `pyproject.toml`，解析结果保存在并提交 `uv.lock`。本地和 CI 的确定性检查必须使用锁文件，禁止同时维护 Poetry、Pipenv 或手写 `requirements.txt` 锁定流程。
- CI 平台：使用 GitHub Actions。CI 必须调用仓库内与本地相同的检查命令，不得在工作流中另写一套只在 CI 中存在的验证逻辑。
- 外部 AI 数据规则：默认只向外部 AI API 发送完成当前任务所必需的最少内容，并在产品中提供关闭外部发送的选项。关闭后必须明确告知哪些功能会受限，不得绕过用户选择继续外发。
- AI 供应商接口：产品必须允许用户配置受支持的 AI API，并根据所选 API 使用对应的供应商适配器和模型能力；核心业务逻辑不得直接绑定单一供应商。
- 首批 AI 供应商：OpenAI（GPT）、DeepSeek、Moonshot AI（Kimi）。每家供应商必须有独立适配器和能力声明；即使接口格式部分兼容，也不得假设模型参数、工具、状态管理或错误语义完全一致。
- API 密钥保存：本地阶段使用 Windows 凭据管理器保存密钥，SQLite 只保存不可用于直接调用的凭据引用和非敏感元数据。后端不得把密钥原文返回给前端或写入日志。
- 对话模型锁定：创建 AI 对话时必须确定并保存该对话使用的供应商、模型和凭据引用。在当前对话结束前，不得切换该对话的 API、供应商或模型，也不得因失败而静默降级到其他模型。
- 对话结束触发方式：
  - 用户点击“结束对话”，并等待当前请求完成或取消；
  - AI 生成的学习计划通过后端校验并成功保存；
  - 默认连续 2 小时无操作；用户可以在设置中修改该时限；
  - 系统检测到符合用户确认规则的 AI 对话异常。
  网络超时属于会结束对话的异常，不得自动重试后继续原对话。其他已确认的致命异常包括密钥失效、权限不足、模型被下架、响应格式持续错误和内部对话状态损坏。供应商限流或临时服务端错误可以提示后重试，但不得切换供应商或模型；模型拒绝回答或安全拦截不视为系统异常。对话结束后只能创建使用其他配置的新对话，不得篡改原对话的供应商或模型记录。
- 身份验证策略：本地验证阶段不做账号登录，打开本地网站即可使用；互联网部署前必须增加合适的身份验证和访问控制。
- 代码运行阶段：先开发访谈、规划、知识来源和学习面板；到编程练习阶段再经用户确认安装 Docker/WSL 并实现隔离代码运行，不得在此之前直接执行来自产品界面的任意代码。
- 第四里程碑实施顺序：先实施 4A；4B 必须另行确认。4A 只完成 `algorithm@0.2.0` 共同主干入口的一条完整纵向切片，不执行代码、不接收文件上传、不读取用户任意本地路径，也不调用外部 AI。
- 算法技能包版本：新增 `algorithm@0.2.0` 草稿并保留 `0.1.0`。`0.2.0` 必须重新完成诊断，不迁移 `0.1.0` 诊断；默认共同主干入口切换到 `0.2.0`。`0.1.0` 立即关闭新诊断、规划和学习执行入口，已有活动诊断及草稿规划以明确原因和审计冻结为只读，继续服务历史引用。
- 技能包版本不可变：`0.2.0` 首次被持久化诊断、规划或学习记录引用时必须锁定 manifest 和受管内容摘要；此后同一版本号不得原地修改，教学内容、复习策略或其他受管内容变化必须发布新的技能包补丁版本。
- 第四里程碑规划选择：学习执行可以显式选择同一精确技能包版本的任意历史 `saved_preview`，不要求属于最新诊断；界面必须展示它是否早于最新诊断或规划并记录用户选择。执行记录进入终态后，用户可以明确确认并复用同一不可变规划创建新记录。
- 第四里程碑状态边界：首轮学习完成只能进入 `retention_pending`；延迟复习失败时保持该状态，追加纠错和新复习；成功后才能进入 `completed`。`completed` 只表示本次流程完成，不表示掌握；显式 `ended` 不可恢复，但所有历史必须保留。
- 第四里程碑证据边界：4A 的结构化诊断信号以确定性规则影响补救活动；六维展示必须把证据等级与人工、保持、来源待办标记分开，待办不得覆盖原证据。普通自述纠错和用户自评只能产生有限证据；只有确定性结构检查或预置错误纠错任务才能产生 `supported`。4A 不提供独立人工审核通过能力，也不产生整体 `scope_criteria_met`、`verified` 代码能力或不受限的 `retained` 证据。
- 第四里程碑来源边界：来源变化在 4A 中只生成确定性元数据差异和 `source_review_pending` 标记，不生成实质内容影响报告、补充学习单元或版本迁移。
- 第四里程碑活动推进：阅读活动确认后可以完成；确定性练习必须通过或完成可确定性验证的纠错路径；解释、代码、迁移和作品文本通过结构校验后可以继续，但不得据此声称内容正确。
- 第四里程碑交付状态：4A 已通过 PR #2 合并到 `main@953d2d2`，合并后 GitHub Actions 运行 `30420229605` 全部成功；4B 仍须项目所有者另行确认，不能因 4A 合并而视为已授权。
- 第五里程碑阶段边界：允许不进行任何外部调用的 5A 先行。5A 只能使用本地已有证据、版本化规则和明确标注的合成市场快照，不调用外部 AI、市场 API 或远程来源，不创建付费资源，也不产生投递、发布、联系、交易等对外动作。
- 第五里程碑 5B 门禁：真实市场研究和持续更新必须等待首批 AI 模型、目标市场范围、来源获取与更新方案、费用预算及停止条件全部确认；缺少任一决定时保持阻塞，不得用默认值、模型记忆或临时抓取替代。
- 第五里程碑实验门禁：真实算法交付类实验必须等待经单独授权的 4B，或项目所有者确认的其他独立验证证据。4A 的文本提交、自评、结构校验和有限证据不能替代该门禁。
- 当前未决定：
  - 三家供应商的首批模型清单、目标市场范围、内容抓取/索引/更新方案和第五里程碑预算；
  - 5C 的首个重点路径、最低证据阈值、可接受的独立验证形式、外部动作边界和实际收入记录策略；
  - 互联网部署平台和具体预算；
  - Logo、商标、公开品牌资产和公开发布计划；
  - 初版视觉方向在真实产品界面中的最终取舍。

不得将未决定事项擅自变成既定方案。

## 3. 与用户协作的硬性规则

- 不得猜测用户意图、缺失需求、预算、技术偏好或产品优先级。
- 当缺失信息会实质影响产品行为、架构、成本、隐私、安全或后续返工时，必须停止相关实施，并向用户提出具体、容易回答的问题。
- 提问时说明：
  1. 哪一点尚不明确；
  2. 它会影响什么；
  3. 可选择的方案及主要差异；
  4. 推荐方案和推荐理由，但最终决定由用户作出。
- 必须清楚区分已确认事实、基于证据的推断、建议和待确认事项。
- 遇到用户陈述、项目文档、代码实现、Git 状态或验证证据之间存在分歧时，必须先停止会改变仓库或运行环境的操作，向项目所有者说明各方证据、时间、影响、可选处理方式和推荐方案，并询问项目所有者；在得到答复前不得自行选择一种解释、修改代码、终止进程、运行可能写入状态的验证、提交或推送。
- 不得以“行业惯例”代替用户确认关键产品决策。
- 不得擅自扩大任务范围、增加主要功能或进行无关重构。
- 可以执行范围明确、可逆且不会引入产品决策的常规实现步骤，但必须报告结果。

## 4. 知识来源与内容可信度

学习内容必须可追溯，不能仅凭模型记忆生成。

- 优先级从高到低通常为：
  1. 官方标准、规范、政府或监管机构资料；
  2. 官方文档和原作者资料；
  3. 同行评审论文、权威教材和专业组织资料；
  4. 高信誉机构、大学和公认专家资料；
  5. 其他来源只能作为补充，不得成为关键结论的唯一依据。
- 对可能变化的信息，必须记录来源、发布日期或版本、检索日期。
- 重要结论应尽量由两个相互独立的高质量来源交叉验证。
- 来源相互冲突时，不得静默选择其中一个；必须呈现冲突、解释可信度判断，并在必要时请用户决定。
- 每个学习单元应能够关联原始来源、证据强度和最近复核时间。
- 引用或整合内容时必须尊重版权、许可、访问条款和合理使用边界；默认保存摘要、结构化知识和出处，而不是未经许可复制全文。
- 不得伪造引用、作者、论文、链接、版本、市场数据或验证结果。
- 无法验证的信息必须明确标注为“未验证”，不能包装成事实。

## 5. 掌握程度规则

系统必须使用可观察证据评估掌握程度，至少考虑：

- 知识理解：能解释关键概念及其联系；
- 操作能力：能独立完成代表性任务；
- 迁移能力：能在陌生情境中应用技能；
- 作品证据：存在可检查的真实项目或成果；
- 保持程度：经过间隔复习后仍能完成测试或任务；
- 纠错能力：能发现问题、解释原因并完成修正。

不得因为用户完成课程、观看内容或通过单次简单测验，就宣称其“完全掌握”。

“掌握”必须针对明确的能力范围和评估标准，而不是对整门技能作无限范围的绝对声明。

## 6. 变现建议规则

- 就业、接单和产品化建议必须基于用户实际能力证据、作品、时间条件和可验证的市场信息。
- AI 可以比较三条路径并给出推荐，但必须展示判断依据、风险、前置条件和不确定性。
- 不得保证工作机会、订单、收入金额或投资回报。
- 市场价格、平台规则、岗位需求和法律税务信息属于易变化或高风险信息，使用前必须检索最新权威来源。
- 在没有足够掌握证据时，不得为了“尽快变现”建议用户提供超出其能力范围的服务。

## 7. 产品设计原则

- 学习路径围绕“诊断 → 学习 → 练习 → 项目 → 评估 → 复习 → 变现实验”形成闭环。
- 诊断阶段采用自适应访谈：AI 根据已知信息选择下一个最有价值的问题，并结合必要的知识测试或编程任务判断真实水平。
- 自适应访谈必须显示提问原因，允许用户跳过、不确定或纠正答案；不得把未经验证的回答永久当成事实。
- 当新增回答会显著改变计划时，AI 应更新受影响部分并保留变化说明，而不是无提示地重写整个计划。
- 每个阶段必须有明确目标、完成标准和可检查证据。
- 优先建立最小但完整的闭环，再扩展技能数量或附加功能。
- AI 输出应解释为什么安排某项内容，以及它与最终能力和变现目标的关系。
- 用户应能查看、修改或否决 AI 生成的学习计划。
- 所有自动更新应保留变更记录，避免知识内容在无提示的情况下发生不可追踪的变化。

## 8. 架构和开发约束

- 在用户确认技术栈之前，不得初始化框架、选择数据库、购买服务、创建云资源或添加基础性依赖。
- 作出架构建议前，先说明需求、备选方案、成本、隐私影响、锁定风险和推荐理由。
- 秘钥、令牌和个人敏感信息不得写入代码或提交到仓库。
- 外部 AI 请求必须经过统一的隐私过滤和发送开关；记录发送目的、数据类别、模型供应商和时间，但日志不得保存秘钥或不必要的原始敏感内容。
- 外部内容、AI 输出和用户输入都必须视为不可信输入，并在相应边界进行验证。
- 涉及外部来源抓取时，必须考虑访问条款、robots 规则、速率限制、版权和内容删除机制。
- 数据模型必须支持来源追踪、版本记录、证据记录和评估历史。
- 仓库内置技能包注册表与文件系统必须严格一致；缺失、未注册、校验和不符、依赖冲突或循环依赖都必须阻止应用启动和 CI 通过。
- 未来的无效远程第三方技能包不得拖垮核心服务，必须进入只读隔离区。系统应通知用户，并向当前由用户选择的 AI 提交经过安全净化的隔离审查材料，由 AI 给出修复、拒绝或后续教学安排建议；AI 不得执行隔离包、绕过重新验证或自行激活。
- 技能包生命周期必须支持与状态独立的 `availability: suspended`，用于安全、法律或数据完整性事件中的紧急停用，并保留完整审计记录。
- `deprecated` 技能包版本必须继续服务所有引用它的进行中计划，直到这些计划完成。新版内容只能通过单独版本锁定的补充学习单元或显式迁移加入，不能改写原计划的锁文件；用户必须收到变更说明。
- 新版补充学习单元必须先由 AI 生成差异、必要性、工作量和插入位置说明，得到用户确认后才能加入学习计划。用户之后可以撤销补充单元并要求 AI 重新规划；撤销和重规划都必须保留审计记录。
- 学习计划必须锁定根技能包、全部传递技能依赖、运行环境摘要、引擎协议和 Runner 协议的精确版本。
- FastAPI 进程不得直接导入或执行技能包中的评分器、自定义校验脚本或用户代码。所有可执行内容必须通过版本化、资源受限的隔离 Runner 协议调用。
- 仓库采用 pnpm workspace 管理 JavaScript 项目；根目录只允许存在 `pnpm-lock.yaml`，不得提交 `package-lock.json`、`yarn.lock` 或 `bun.lock`。
- FastAPI 应用位于 `apps/api`，Next.js 应用位于 `apps/web`；跨边界协议位于 `contracts`，不得从前端直接导入后端内部模块或反向导入。
- SQLite 数据文件、测试数据库、缓存、虚拟环境和构建产物不得提交。数据库访问必须集中在后端边界，前端不得直接读取 SQLite 文件。
- 后端数据访问采用 SQLAlchemy 2，数据库迁移采用 Alembic。应用启动不得使用 `create_all` 绕过迁移历史；Schema 变更必须通过版本化迁移完成。
- API 契约以 FastAPI 生成的 OpenAPI 文档为事实来源。生成后的契约制品必须提交，并通过无漂移检查确保代码与制品一致。
- 根目录脚本是本地与 CI 的统一入口。新增 CI 检查时，必须先提供可在本地运行的等价命令。
- 依赖安装和质量检查必须使用锁定模式：前端使用 `pnpm install --frozen-lockfile`，后端使用 `uv sync --locked`；发现锁文件漂移时直接失败。
- 任何技能包、合同或注册表校验失败时，FastAPI 不得以“部分可用”方式绕过仓库内置内容错误启动。
- 修改代码前先检查现有结构和约束；保持变更范围最小且目的明确。
- 不得声称构建、测试、检查或部署成功，除非实际执行并取得成功结果。

## 9. 验证要求

任何实现都应根据风险进行相称验证：

- 核心学习路径和评估逻辑必须有自动化测试；
- 来源追踪、版本更新和冲突处理必须有测试；
- 关键用户流程应有端到端验证；
- 涉及界面时应检查不同屏幕尺寸、空状态、错误状态、加载状态和基本无障碍；
- 无法执行的验证必须在交付说明中明确列出，不能省略不报。

## 10. 工作流程

对非简单任务按以下顺序进行：

1. 检查仓库和现有约束；
2. 列出已知条件、未知条件和风险；
3. 对会影响方向的未知条件先向用户提问；
4. 提交范围明确的实施方案；
5. 获得必要确认后实施；
6. 执行验证；
7. 报告完成内容、验证结果、遗留风险和下一项待确认决定。

### 10.1 项目接管与只读阅读规则

当项目所有者要求先阅读、接管或盘点项目，而没有同时授权继续实施时，默认只做只读检查。不得因此运行会创建数据库、缓存、构建产物或测试状态的命令，也不得终止服务、清理文件、修改代码、提交或推送。

为避免重复本项目在 2026-07-28 接管阅读中出现的无效尝试，后续代理应按以下方式检查：

- 先使用 `git status --short --branch`、`git diff --stat`、`git diff --name-status` 和 `git ls-files --others --exclude-standard` 确认真实变更范围。不要先递归枚举整个仓库，否则 `.pnpm-store`、`node_modules`、`.next`、`.venv` 和各类缓存会制造大量噪声并导致输出截断。
- 文本或文件搜索优先尝试 `rg`；如果当前 Windows 环境返回“拒绝访问”，记录一次失败后立即改用限定目录的 `Get-ChildItem` 与 `Select-String`，不要反复尝试同一不可用命令。
- 大文件和大范围 diff 应按文件或行号区间分批读取。并行读取结果发生截断时，不能把截断内容当作已完整审阅，必须改为读取相关文件的关键区段。
- 如果 `Get-CimInstance Win32_Process` 因权限不足失败，使用 `Get-Process` 核对进程路径和启动时间，使用 `netstat -ano` 或 `Get-NetTCPConnection` 核对监听端口；必要时只读访问本地 `/openapi.json` 比较接口版本。不得为了确认版本调用会写入数据库或触发外部请求的接口。
- 进程 ID 和端口属于易变化现场信息。任何后续终止操作前必须重新核对进程路径、启动时间、监听端口和项目关联，不能直接使用历史记录中的 PID。
- `.pytest_cache`、Playwright completion 文件、`test-results`、构建目录和日志只能证明某个时间点留下的结果，不能代替当前代码的重新验证。报告时必须标注其时间和“历史证据”属性；只有实际重新执行并成功，才能声明当前检查通过。
- 用户交接说明与磁盘证据不一致时，必须按照第 3 节规则先询问项目所有者，不能静默采用任一方。

### 10.2 2026-07-28 端到端验证接管记录

以下内容是当日只读盘点结论，用于避免下一位代理重复排查；它不是永久状态，继续操作前必须重新核对：

- 第三里程碑已有 24 个已跟踪文件修改和 15 个未跟踪新文件，尚未提交；`main` 与 `origin/main` 当时均位于 `a49c665`。
- 后端 pytest 缓存列出 10 项测试且 `lastfailed` 为空；这只是历史证据。前端 lint、类型、测试和构建通过来自交接说明，未在本次只读盘点中重新执行。
- 旧 API 曾监听固定端口 `8000`，其 OpenAPI 不包含第三里程碑接口；动态端口版 API 曾监听 `33747`，其 OpenAPI 包含新接口。固定端口服务与测试服务混用确实能够造成新接口 HTTP 404。
- 动态端口修改已经存在于 `tools/run_e2e.mjs` 和 `apps/web/playwright.config.ts`，但当时尚未取得成功的 E2E 结果。最后一个可见 Playwright completion 文件在 `2026-07-28 00:40:58` 记录为 `failed`，桌面端和移动端均未显示规划标题。
- E2E 测试使用即时 `isVisible()` 决定是否点击“生成本地规划预览”，可能在页面仍处于加载状态时跳过点击。测试期望标题为“算法共同主干入门规划预览”，技能包模板实际标题为“算法共同主干入口规划预览”；即使端口问题解决，这一断言也需要统一。
- Windows 上的 E2E 进程清理当时没有可靠回收固定端口和动态端口 Python 进程，因此 `tools/run_e2e.mjs` 的停止逻辑仍需验证。
- 完整 `release-readiness` 当时没有通过。README 中“受限预览已完成”只能表示实现范围，不能表示发布就绪；在取得新鲜的完整成功结果前，应明确写成“实现完成、验证未完成”或等价表述。

建议处理顺序是：在获得项目所有者确认后重新识别并终止仅属于本项目的残留服务；修正 E2E 等待逻辑、标题断言和 Windows 清理逻辑；先运行 `check:e2e`；成功后运行完整 `release-readiness`；根据真实结果更新进度文档；最后再由项目所有者决定是否提交和推送。不得跳过确认直接执行这一顺序。

后续解决记录（2026-07-28）：项目仍保持 Node 24 LTS 基线。Codex PowerShell 的裸 `node` 曾从系统 PATH 解析为已经 EOL 的 Node 25，而 Codex 内置 pnpm 自身使用 Node 24，造成混合运行时；后续验证通过在单次命令中把 Codex 内置 Node 24 放到 PATH 最前解决。当前沙箱会拒绝 `taskkill`，E2E 启动器已在其失败时使用 Node `process.kill()` 终止真实监听 PID，并验证动态端口释放。修正等待逻辑、标题断言、缺失类型导入和测试明文凭据后，`check:e2e` 正常退出且桌面/移动测试通过，完整本地 `release-readiness` 也已通过。该结论不等于第三里程碑远程 GitHub Actions 已通过；推送后的远程 CI 仍需单独验证。

### 10.3 实时更新待办事项

根目录 `TODO.md` 是仓库级未完成事项的唯一账本。所有代理必须遵守：

- 开始非简单实施前先读取 `TODO.md`，核对当前门禁、阻塞、范围和下一步；
- 在任务范围内发现新的必要工作时，立即登记编号、状态、事项、前置条件或完成标准，不得只留在对话或临时笔记中；
- 待办状态、下一步、阻塞原因或验证证据发生变化时，必须在同一工作任务内同步更新，不得等到后续代理补记；
- 只有实际实现并完成相称验证后才能标记 `已完成`，同时记录可复核的命令结果、制品、提交或文档证据；历史缓存和交接陈述不能单独作为完成证据；
- `阻塞` 必须写明真实外部依赖、用户决定或失败证据；解除阻塞后应立即调整状态；
- 事项被取消、移出范围或替代时，不得静默删除，应保留 `已取消` 状态、原因和日期；
- 每次修改待办时更新文件顶部日期，并在维护记录中追加简短变更说明；
- `TODO.md` 只记录和追踪工作，不构成实施、提交、推送、部署、购买或扩大范围的授权；
- `TODO.md` 与用户指令、项目文档、代码、Git 状态或验证证据不一致时，必须按第 3 节停止相关实施并询问项目所有者。

首个开发里程碑必须按“最小项目骨架 → 本地确定性检查 → CI 基线 → 业务功能”的顺序执行。CI 基线未通过前，不得开始实现自适应访谈、学习规划等业务功能。

本地与 CI 的首批稳定检查入口至少包括：

- `check:markdown`：Markdown 基线质量；
- `check:structure`：仓库结构、受管文件和禁止锁文件；
- `check:registry`：注册表、manifest、内容完整性和依赖图；
- `check:contracts`：JSON Schema 与 OpenAPI 契约；
- `check:api`：FastAPI lint、格式、类型和测试；
- `check:web`：Next.js lint、类型、测试和构建；
- `check:contract-drift`：重新生成契约后不得出现差异；
- `check:secrets`：仓库敏感信息基线扫描；
- `release-readiness`：汇总所有适用检查。

所有检查必须有真实校验对象，不能使用永远成功的占位命令。工作流不得使用路径过滤跳过必需检查；即使某一部分没有变化，稳定检查名称也必须产生明确结果。

## 11. 规则来源

本文件为本项目专门编写，结构参考了以下公开 GitHub 项目：

- AGENTS.md 开放格式：<https://github.com/agentsmd/agents.md>
- “需求不清时停止并提问、不得猜测”的变更安全原则：
  <https://github.com/telemt/telemt/blob/main/AGENTS.md>

这些来源只用于形成通用协作和工程约束；本文件未沿用其项目特定的技术栈、命令或架构决定。
