# AGENTS.md

本文件是本项目中所有 AI 编码代理必须遵守的项目级约束。它适用于整个仓库。

## 1. 项目使命

构建一个目前仅供项目所有者本人使用的 AI 学习系统，帮助用户：

1. 学习一项技能；
2. 通过测验、实践和真实作品证明掌握程度；
3. 将已掌握的技能转化为就业、自由职业接单或产品化收入的能力；
4. 持续追踪知识变化，并更新学习路径。

本项目不能把“看完内容”等同于掌握技能，也不能把预测的变现机会描述为确定收入。

## 2. 已确认的产品决定

- 初始用户：仅项目所有者本人。
- 初始商业模式：免费验证，不设计付费功能，除非用户之后明确要求。
- 内容模式：AI 从全球范围内尽可能权威的来源检索、整合、校验和动态维护学习内容。
- 变现方向：系统根据技能、掌握证据和市场信息评估就业、自由职业接单、产品化三条路径，并尽可能同时支持。
- 技能扩展方式：先完整做好一个技能；底层采用可复用的通用学习引擎，后续以独立“技能包”的方式逐个增加新技能，不能把首个技能硬编码进通用流程。
- 首个技能：算法。
- 产品名称：云奕学。
- 初版视觉方向：允许 AI 先提出并记录可修改的视觉方向提案，后续可以根据实际使用体验调整。提案不得被描述为不可变的最终品牌规范；Logo、商标、公开品牌资产和大规模视觉改版仍须在实际制作前确认范围。
- 仓库许可证：项目所有者拥有版权的项目代码和文档采用 PolyForm Noncommercial License 1.0.0，版权人为“云奕学项目所有者（joonas-001）”。该许可证只允许规定范围内的非商业使用，不得标注为 MIT License 或 OSI 开源许可证。商业使用必须另行取得版权所有者授权；第三方依赖和引用内容继续适用各自的许可证与条款。
- 算法技能范围：采用综合路线，以数据结构与算法为共同主干，并覆盖工程应用、求职面试、算法竞赛和算法理论。应避免四条路线重复教授相同基础内容。
- 算法路线组织：用户完成并通过共同主干的阶段评估后，再逐步解锁工程应用、求职面试、算法竞赛和算法理论四个分支；不得在基础不足时同时堆叠四条路线。
- 用户当前基础：了解一些 Python 和 C/C++，但基础尚不扎实，学习路径必须先通过诊断确认缺口并补牢必要的编程基础。
- 算法实践语言：以 C++ 为主，同时支持 Python；需要说明同一算法在两种语言中的实现差异、标准库使用方式和性能差异。
- 算法学习目标：系统掌握算法理论，提高实际开发能力，具备求职面试能力，并能够参与算法竞赛。
- 用户学习时间：每天约 2 小时。计划必须能拆分为可执行的每日任务，并根据实际完成情况动态调整，不能假设用户每天都能严格按计划完成。
- 用户数学基础：只学过微积分且掌握不牢。算法路线必须先诊断所需数学基础，并按需补充离散数学、逻辑、组合数学、概率和算法分析所需内容，不能假定用户已经具备这些知识。
- 效果与成本偏好：前期优先保证学习和 AI 规划效果，再评估和控制模型、检索、存储及部署费用；任何可能持续产生费用的方案仍须提前说明计费方式和大致成本，不能擅自开通付费资源。
- 计划制定方式：AI 在生成学习计划前，应根据用户目标和已有回答进行动态、递进式访谈。后续问题可以由 AI 合理规划，不限于固定问卷；当信息足够时停止提问并解释规划依据，避免无意义或重复提问。
- 运行与部署策略：先在用户电脑本地完成首版开发和验证；通过约定的验证标准后，再制定互联网部署方案。未经用户后续确认，不得提前创建线上服务或部署公开网站。
- 首版本地技术栈：前端使用 Next.js，后端使用 FastAPI，本地数据使用 SQLite。互联网部署前可以重新评估数据库和运行架构，但迁移必须保留现有学习、来源和评估数据。
- 前端包管理器：使用 pnpm。不得混用 npm、Yarn 或 Bun，也不得生成其他包管理器的锁文件。
- JavaScript 运行时基线：Node.js 24 LTS。根目录必须声明并锁定该主版本；本地开发和 CI 使用同一主版本。
- Python 运行时基线：Python 3.14.3。不得在未验证兼容性和迁移影响前擅自改变该版本。
- 如果必要的 Python 依赖不支持 Python 3.14，必须停止实施并报告具体依赖、失败证据和可选处理方式；未经用户确认不得降低 Python 版本。
- Python 项目与依赖管理：使用 uv，依赖声明保存在 `pyproject.toml`，解析结果保存在并提交 `uv.lock`。本地和 CI 的确定性检查必须使用锁文件，禁止同时维护 Poetry、Pipenv 或手写 `requirements.txt` 锁定流程。
- CI 平台：使用 GitHub Actions。CI 必须调用仓库内与本地相同的检查命令，不得在工作流中另写一套只在 CI 中存在的验证逻辑。
- 外部 AI 数据规则：默认只向外部 AI API 发送完成当前任务所必需的最少内容，并在产品中提供关闭外部发送的选项。关闭后必须明确告知哪些功能会受限，不得绕过用户选择继续外发。
- AI 供应商接口：产品必须允许用户配置受支持的 AI API，并根据所选 API 使用对应的供应商适配器和模型能力；核心业务逻辑不得直接绑定单一供应商。
- 首批 AI 供应商：OpenAI（GPT）、DeepSeek、Moonshot AI（Kimi）。每家供应商必须有独立适配器和能力声明；即使接口格式部分兼容，也不得假设模型参数、工具、状态管理或错误语义完全一致。
- API 密钥保存：本地阶段使用 Windows 凭据管理器保存密钥，SQLite 只保存不可用于直接调用的凭据引用和非敏感元数据。后端不得把密钥原文返回给前端或写入日志。
- 对话模型锁定：创建 AI 对话时必须确定并保存该对话使用的供应商、模型和凭据引用。在当前对话结束前，不得切换该对话的 API、供应商或模型，也不得因失败而静默降级到其他模型。
- 对话结束触发方式：
  - 用户点击“结束对话”，并等待当前请求完成或取消；
  - AI 生成的学习计划通过后端校验并成功保存；
  - 默认连续 2 小时无操作；用户可以在设置中修改该时限；
  - 系统检测到符合用户确认规则的 AI 对话异常。
  网络超时属于会结束对话的异常，不得自动重试后继续原对话。其他已确认的致命异常包括密钥失效、权限不足、模型被下架、响应格式持续错误和内部对话状态损坏。供应商限流或临时服务端错误可以提示后重试，但不得切换供应商或模型；模型拒绝回答或安全拦截不视为系统异常。对话结束后只能创建使用其他配置的新对话，不得篡改原对话的供应商或模型记录。
- 身份验证策略：本地验证阶段不做账号登录，打开本地网站即可使用；互联网部署前必须增加合适的身份验证和访问控制。
- 代码运行阶段：先开发访谈、规划、知识来源和学习面板；到编程练习阶段再经用户确认安装 Docker/WSL 并实现隔离代码运行，不得在此之前直接执行来自产品界面的任意代码。
- 第四里程碑实施顺序：4A 已先行完成；4B 已于 2026-08-01 获得另行确认。4A 只完成 `algorithm@0.2.0` 共同主干入口的一条完整纵向切片，不执行代码、不接收文件上传、不读取用户任意本地路径，也不调用外部 AI。
- 算法技能包版本：新增 `algorithm@0.2.0` 草稿并保留 `0.1.0`。`0.2.0` 必须重新完成诊断，不迁移 `0.1.0` 诊断；默认共同主干入口切换到 `0.2.0`。`0.1.0` 立即关闭新诊断、规划和学习执行入口，已有活动诊断及草稿规划以明确原因和审计冻结为只读，继续服务历史引用。
- 技能包版本不可变：`0.2.0` 首次被持久化诊断、规划或学习记录引用时必须锁定 manifest 和受管内容摘要；此后同一版本号不得原地修改，教学内容、复习策略或其他受管内容变化必须发布新的技能包补丁版本。
- 第四里程碑规划选择：学习执行可以显式选择同一精确技能包版本的任意历史 `saved_preview`，不要求属于最新诊断；界面必须展示它是否早于最新诊断或规划并记录用户选择。执行记录进入终态后，用户可以明确确认并复用同一不可变规划创建新记录。
- 第四里程碑状态边界：首轮学习完成只能进入 `retention_pending`；延迟复习失败时保持该状态，追加纠错和新复习；成功后才能进入 `completed`。`completed` 只表示本次流程完成，不表示掌握；显式 `ended` 不可恢复，但所有历史必须保留。
- 第四里程碑证据边界：4A 的结构化诊断信号以确定性规则影响补救活动；六维展示必须把证据等级与人工、保持、来源待办标记分开，待办不得覆盖原证据。普通自述纠错和用户自评只能产生有限证据；只有确定性结构检查或预置错误纠错任务才能产生 `supported`。4A 不提供独立人工审核通过能力，也不产生整体 `scope_criteria_met`、`verified` 代码能力或不受限的 `retained` 证据。
- 第四里程碑来源边界：来源变化在 4A 中只生成确定性元数据差异和 `source_review_pending` 标记，不生成实质内容影响报告、补充学习单元或版本迁移。
- 第四里程碑活动推进：阅读活动确认后可以完成；确定性练习必须通过或完成可确定性验证的纠错路径；解释、代码、迁移和作品文本通过结构校验后可以继续，但不得据此声称内容正确。
- 第四里程碑交付状态：4A 已通过 PR #2 合并到 `main@953d2d2`，合并后 GitHub Actions 运行 `30420229605` 全部成功；4B 已由项目所有者于 2026-08-01 单独授权，代码纵向切片、WSL2／Docker Desktop 实机安装和真实 Runner 十项安全与资源限制矩阵均已完成。合并前安全与证据审查修复后，Node 24.14.0 下最新完整 `release-readiness` 通过 82 项后端、15 项前端、生产构建、桌面／移动 E2E、契约无漂移和密钥扫描；真实 Runner 十项矩阵也重新全部成功。审查修复提交 `d2836ce` 的 PR CI 运行 `30696525680` 共 21 项作业全部成功；PR #6 已合并到 `main@0efdb4d`，合并后 main CI 运行 `30696741469` 共 21 项作业全部成功。4B 基础设施验证不自动解除 5C 门禁。
- 第四里程碑 4B 决定：项目所有者于 2026-08-01 明确授权 4B。隔离方案采用个人版
  Docker Desktop 的 WSL2 Linux 容器后端；C++ 为 GCC 15.2.0，Python 学习运行时为
  3.14.3；历史 Runner `1.0.0` 保留，实际执行使用 `1.1.0`；同一时刻最多一个任务，
  编译限制为 15 秒、768 MB、1 CPU、64 进程，执行限制为 3 秒、256 MB、1 CPU、
  32 进程，源码、输入和合并输出分别最多 64 KiB。
- 第四里程碑 4B 安全边界：执行时必须断网、使用非 root、只读根文件系统、受限临时
  文件系统、移除全部 Linux capabilities、禁止提权并保持默认 seccomp；不得挂载 Docker
  Socket、数据库、凭据、仓库或用户任意路径。练习时禁止拉取镜像，镜像与平台必须锁定
  摘要；超时、异常和启动恢复只能清理带云奕学专用标签的任务资源。
- 第四里程碑 4B 版本和证据边界：不得原地修改 `algorithm@0.2.0`，4B 教学与运行内容
  发布为 `algorithm@0.2.1` 草稿且不自动迁移历史。只有锁定运行时和确定性测试全部通过
  才能为对应操作范围产生 `verified`；只有延迟 Runner 复测通过才能产生对应范围的
  `retained`。两者均不得表示整体掌握、自动解锁分支或自动解除 5C 门禁。
- 算法技能包来源修复：项目所有者于 2026-08-04 授权修复 cppreference 来源类型。
  已引用的 `algorithm@0.2.1` 保持不可变并关闭新入口；修复以 `algorithm@0.2.2`
  草稿发布，cppreference 标记为社区维护参考资料，默认诊断、规划和学习入口切换到
  `0.2.2`。`0.2.1` 继续服务历史引用，不自动迁移。
- Runner 返回的顶层 `passed` 不能直接成为证据。FastAPI 证据边界必须独立校验结果
  Schema、请求审计 ID、源码摘要、运行时 ID／版本／精确镜像摘要、实际观察镜像摘要、
  完整测试集合、逐项退出码／输出／截断状态及固定安全声明；任一矛盾只能记录为
  `protocol_invalid` 基础设施错误，不得产生 `verified` 或 `retained`。
- Runner 的源码、测试输入、期望输出和合并输出上限均按 UTF-8 字节计算，不能只依赖
  JSON Schema 的字符长度。迁移回滚删除 `verified`／`retained` 后必须把受影响快照重置
  为无证据，不能在不存在低等级原始证据时自动降格为 `supported`。
- 第四里程碑 4B 本机状态：2026-08-01 检查确认内存 31.46 GB，C 盘可用 101.85 GB，
  D 盘 NTFS 可用 136.14 GB；Docker/WSL 数据根目录使用
  `D:\CloudStudy\DockerData`，项目所有者根据实测占用将硬预算明确调整为 6 GB，启动
  安装前至少 8 GB 可用的门槛不变。项目所有者已启用固件虚拟化，任务管理器显示已启用
  且 Windows 报告 `HypervisorPresent=True`。WSL 2.7.11、Docker Desktop 4.84.0 和
  Engine 29.6.2 已安装；GCC 15.2.0 与 Python 3.14.3 按精确摘要完成版本核验。
  真实验证时 D 盘可用 131.79 GB，数据根占用 5.256 GB；`pnpm check:runner-live`
  的 C++／Python、断网、只读根、无宿主仓库、无 Docker Socket、进程、超时、合并
  输出和内存限制十项探针全部通过，并确认无项目标签容器残留。
- 第五里程碑阶段边界：项目所有者先授权 5A，之后已单独授权 5B，并于 2026-08-01
  单独授权 5C 代码实施。5A 只能使用本地已有证据、版本化规则和明确标注的合成市场快照，
  不调用外部 AI、市场 API 或远程来源，不创建付费资源，也不产生投递、发布、联系、交易
  等对外动作。
- 第五里程碑目标选择：用户在运行时选择当前目标，系统不得默认用户希望变现。目标可以是就业、接单、产品化，也可以是考试、纯学习或其他用户自定义目标；非变现目标不得被强制进入市场比较，应明确显示变现准备比较当前不适用，并继续展示能力证据、缺口和限制。
- 第五里程碑 5A 交付状态：实现提交 `826d732` 已通过 PR #3 合并到
  `main@2f847c6`；PR 头提交 `f92cab1` 的 GitHub Actions 运行 `30434488897`
  共 19 项作业全部成功，合并后 `main` 的运行 `30523076260` 共 19 项作业全部成功。
- 第五里程碑 5B 门禁：真实市场研究和持续更新必须等待首批 AI 模型、目标市场范围、来源获取与更新方案、费用预算及停止条件全部确认；缺少任一决定时保持阻塞，不得用默认值、模型记忆或临时抓取替代。
- 第五里程碑 5B 决定：上述门禁已由项目所有者于 2026-07-29 解除。5B 只允许 DeepSeek 官方 API 的 `deepseek-v4-flash`；首版市场为中国大陆中文市场，覆盖初级 C++ 后端、算法应用工程、中小客户接单、中文个人开发者与算法学习者；来源只允许政府、监管、标准组织和数据所有方的一手官方 API、数据集、RSS 或许可公开页面；费用硬上限为月 ¥5、日 ¥0.50、单次 ¥0.20，月度 50%/80% 告警，禁止自动充值，价格变化、无法估价或余额不足时停止，不得静默重试或切换模型。
- 第五里程碑 5B 交付状态：代码纵向切片、审查修复以及 `FIX-5B-001` 至
  `FIX-5B-014` 已通过 PR #4 合并到 `main@273c190`。官方来源成功访问保持 7 天刷新
  间隔，失败后冷却 24 小时，首版不允许人工绕过；部分失败重试只访问已到期来源。Node
  24.14.0 下最新完整本地门禁成功，包括 61 项后端测试、14 项前端测试、
  生产构建、桌面/移动 E2E、契约无漂移和密钥扫描。当前目录三条路径只有背景证据能力，
  不能产生确定性市场结论。已真实
  访问国家统计局、工信部与 DeepSeek 官方价格页，并真实调用一次
  `deepseek-v4-flash`；HTTP 200 且模型匹配，但内容结构校验失败，按 usage 保守记账
  ¥0.003597，未生成可采纳结论。PR 头提交 `0dfef1c` 的自动 GitHub Actions 运行
  `30523980823` 共 19 项作业全部成功；合并后 `main` 的运行 `30524441148` 共 19 项
  作业全部成功。修复后的真实模型验证尚未执行，30 天调用间隔和逐次确认不得绕过。
- 第五里程碑实验门禁：真实算法交付类实验必须等待 4B 真实隔离 Runner 验证通过并取得符合范围的独立运行证据，或项目所有者确认的其他独立验证证据。4B 获得授权或仅完成离线代码门禁本身都不能解除该门禁；4A 的文本提交、自评、结构校验和有限证据也不能替代。
- 第五里程碑 5C 重点路径：首版只启用就业路径，聚焦中国大陆中文市场中的初级
  C++ 后端和算法应用工程准备。接单与产品化必须保留通用契约能力，但首版不得创建对应
  实验；后续启用必须另行确认。
- 第五里程碑 5C 分级门禁：证据不足时可以创建 `draft` 并展示缺口；批准本地实验要求
  六维证据齐全且阻断性复核全部清除；记录真实求职动作还要求对应操作能力至少
  `verified`、保持能力至少 `retained`，迁移与作品各有可接受的独立验证记录。证据过期、
  来源待复核或版本锁不一致时必须进入 `blocked` 或 `paused`。所有资格只适用于明确能力
  范围，不得外推为整门算法掌握。
- 第五里程碑 5C 独立验证：接受 4B 锁定 Runner 的确定性验证，以及外部真人评审的手动
  证明记录。真人评审只记录评审者关系、评审范围、量表版本、结论和日期，不上传身份证明
  或其他敏感附件；用户自评和 AI 自评不得充当独立验证。
- 第五里程碑 5C 外部动作边界：产品只生成本地计划、检查清单、可复制材料，并记录用户
  在产品外手动执行的结果。不得自动投递、联系、发布、登录平台、签约或交易；每次真实
  动作都必须由用户明确确认并在外部自行完成。
- 第五里程碑 5C 收入记录：允许可选的本地手动记录币种、税前或含税收入、平台费用、
  直接成本、实收金额、日期和验证等级，不保存合同、账单、银行材料或其他附件。金额默认
  隐藏，支持 JSON/CSV 导出；修改采用追加式修订，删除时清除敏感值并保留无敏感内容的
  审计墓碑。收入记录不得反推掌握程度，也不得用于保证未来收入。
- 第五里程碑 5C 交付状态：就业优先本地实验纵向切片已在
  `codex/milestone-5c` 实现，包括版本化策略与契约、Alembic `0010`、分级证据门禁、
  外部真人评审、用户手动作与结果记录、隐私收入修订／删除／导出、学习回流建议、API 和
  桌面／移动界面。PR #8 已合并到 `main@9928e23`，合并后 GitHub Actions 运行
  `30701908365` 共 21 项作业全部成功。项目所有者于 2026-08-02 明确将当前用户的
  真实求职动作证据补齐设置为阻塞／待解决；代码完成不得解释为该门禁已解除。
- 第五里程碑 5C 时效与真人评审规则：项目所有者于 2026-08-04 确认能力证据和外部
  真人评审最多使用 90 天，市场研究最多使用 7 天，未来日期无效；真人评审必须精确
  绑定当前能力范围并使用受管量表。市场目录必须与精确技能包版本匹配，不得用测试夹具、
  跨版本复用或历史记录绕过。
- 第六里程碑阶段边界：项目所有者于 2026-08-02 已确认完整部署决策包并授权 6A
  代码实施。6A 只允许本地实现部署策略、身份与网络边界、只读凭据适配器、加密备份恢复、
  预检、运维模板、契约、测试和文档；仍不得创建云资源、账号或付费订阅，不得购买域名、
  迁移真实数据、发布互联网服务或实施 6B。
- 第六里程碑 6A 修复与交付状态：项目所有者于 2026-08-04 授权修复审计发现并选择
  仓库启动器。实现 PR #9 已合并到 `main@533d562`，PR CI `31243991424` 和合并后
  主线 CI `31263264784` 均为 22 项成功。Windows 预检兼容修复和本地合成数据库
  加密备份、源库删除、恢复及完整性演练通过 PR #10 合并到 `main@b2b816f`；PR CI
  `31301974590` 和合并后主线 CI `31302076612` 均为 22 项成功。6A 已正式交付完成；
  该结论只表示 6A 交付时的边界；不覆盖项目所有者随后对 6B 的单独授权，也不证明腾讯云、
  Tailscale 或异机恢复已经验证。
- 第六里程碑 6A 决定：个人电脑关机后仍须可访问；候选平台固定为腾讯云轻量应用服务器
  新加坡区单实例 2 vCPU／2 GiB／40 GiB／512 GiB 月流量，数据存放新加坡；只通过
  Tailscale 私有地址和 Microsoft 个人账号精确单账号白名单访问，不开放注册，不购买
  自定义域名。月度费用硬上限 ¥50、预期约 ¥32、50%／80% 告警，禁止自动充值和扩容，
  价格变化、无法估价或结算超限时停止并再次确认。
- 第六里程碑 6A 数据与运行边界：首版保留单实例持久化 SQLite，受管关系数据库迁移为
  后续 TODO；首次私有预发布禁用远程 Runner、真实 AI、真实来源访问和邮件，后续启用
  Runner 必须单独授权。备份至少每日使用离线私钥控制的加密制品，保留 7 个日备份和
  4 个周备份，RPO 24 小时、RTO 4 小时，预发布前完成恢复演练并每季度复验。运维日志
  最少化且保留 7 天，不记录敏感正文；产品审计历史完整保留。详细门禁见
  `docs/architecture/internet-deployment.md`。
- 第六里程碑 6B 当前状态：项目所有者于 2026-08-09 已单独授权合成数据私有预发布、单个
  腾讯云轻量应用服务器新加坡实例创建及对应付费。实例已购买并部署 Ubuntu 24.04 LTS，
  API/Web 服务、Tailscale Tailnet-only 单账号访问、加密备份与恢复演练、预算配置和当前
  范围访问验收已有证据；公网 HTTP、Ping、SSH 规则已禁用。6B 不包含真实数据迁移、真实
  AI、邮件、远程 Runner 或公开发布。
- 第六里程碑 6C 授权与当前状态：项目所有者于 2026-08-15 授权真实数据库加密副本迁移与
  回滚演练，并另行授权远程 Runner 实现和云端验证，验证通过前保持关闭。真实库演练已在
  停止本地写入后完成，47 张表共 54 行的 Schema、逐表语义摘要、外键、事件顺序、内容锁和
  反向恢复一致，临时明文副本未保留，原服务已恢复。远程 Runner 采用 FastAPI 无 Docker
  权限、独立 Unix Socket Broker、候选发布与 live app 分离的边界。精确候选提交
  `f1d78f8` 已在新加坡实例通过 C++／Python、断网、只读根、无宿主仓库、无 Docker Socket、
  进程、超时、合并输出和内存限制十项矩阵；验证结束后 Broker 已停止且保持 `static`，项目
  容器残留为零，生产策略继续 `remote_enabled=false`。最终本地 `release-readiness` 通过
  114 项后端测试、1 项按预期跳过、42 项前端测试、生产构建、桌面／移动 8 项 E2E、契约
  无漂移和密钥扫描。PR #16 已合并到 `main@65529de`，PR CI `31886478208` 和合并后主线
  CI `31886598716` 均为 22 项成功。6C 不授权扩容、生产启用、真实数据云迁移或 6D；云端
  基础设施验证不得充当用户个人 `verified` 或 `retained` 证据。
- 第六里程碑 6D Runner 范围决定：项目所有者于 2026-08-15 确认将已通过 6C 云端验证的
  远程 Runner 纳入 6D 启用范围。该决定只确定 6D 的实施范围，不等于授权执行 6D；在取得
  6D 最终授权并重新通过上线预检前，生产策略必须保持 `remote_enabled=false`，Broker 必须
  保持停止且不得接入 live API。6D 实施时仍须复核精确提交、镜像摘要、Unix Socket 权限、
  十项安全与资源矩阵、容器清理、资源余量和回滚能力，不得因已纳入范围而跳过门禁。
- 第六里程碑 6D 授权与实施状态：项目所有者于 2026-08-21 明确授权实施 6D。授权解除的是
  6D 实施门禁，不解除上线预检、精确提交、真实数据迁移、身份、预算、备份、回滚和观察期
  门禁，也不授权真实 AI、真实来源、邮件、扩容、公开注册或公开发布。实施开始时云端 API、
  Web 和备份计时器正常，Broker 为 `inactive + static`、项目容器残留为零，磁盘和内存余量
  满足继续准备；正式切换前发现 `origin/main@b213a91` 与云端已确认的 7F 提交 `6acdaca` 的应用
  基线不一致。项目所有者随后确认采用独立 6D 基线，并允许推送源码和文档及创建 Draft PR；
  分支 `codex/milestone-6d@cbdbe4a` 的 PR #19 CI 运行 `32444825009` 共 22 项成功。该选择会使
  云端界面从未合并的 7F 状态回到当前主线基线，但不跳过真实库、Runner、身份和回滚门禁。
- 第七里程碑阶段边界：项目所有者于 2026-08-11 确认第七里程碑可在独立
  `codex/milestone-7a` 分支和 worktree 中与第六里程碑并行，不再以 6A、6B、6C、6D
  全部完成作为 7A 启动条件；7A 已完成。项目所有者于 2026-08-12 接受方向 A 组合方案，
  确认 `Leonxlnx/taste-skill` 为原候选上游、7B 零新增依赖，并授权按已记录的最小范围实施
  生产前端。7B 只能建立统一应用外壳、导航、页面上下文和准确的受限入口；不得修改第六
  里程碑工作区、服务器或部署状态，不得安装或执行外部设计技能、复制外部实现、增加依赖、
  改变后端契约或提前实施 7C/7D 业务聚合。7B 已完成统一外壳实现并通过完整本地
  `release-readiness`。PR #12 已于 2026-08-12 合并为 `main@41ce8cc`，合并后主线 CI
  `31565328741` 共 22 项成功。项目所有者随后单独授权 7C 在基于该主线的独立分支实施，
  保持零新增依赖。PR #13 已于 2026-08-13 合并为 `main@c19ccd4`，合并后主线 CI
  `31678268555` 共 22 项成功。项目所有者继而授权基于该主线的独立 7D 分支，保持零新增
  依赖。PR #14 已于 2026-08-14 合并为 `main@809a6c5`，合并后主线 CI `31801013282`
  共 22 项成功。项目所有者于 2026-08-14 授权从 7D 精确提交建立独立 7E 分支，保持零新增
  依赖实施设计系统与无障碍基线；7F 仍未授权。
- 第七里程碑已确认方案：默认首页改为“今日”，一级信息架构固定为“今日、学习、证据、
  目标与行动、收件箱、设置”；桌面端采用统一左侧主导航，移动端采用“今日、学习、证据、
  收件箱、更多”的底部导航，其中收件箱始终与设置同属一级产品能力。实施按 7A 信息架构
  与体验基线、7B 统一应用外壳、7C 今日与收件箱纵向切片、7D 学习与证据中心、7E 设计
  系统与无障碍、7F 体验验收分阶段推进。`taste-skill` 等 GitHub 项目只能作为候选方法
  来源，必须核验精确提交、许可证、安全、适用范围和锁定方式，不得照搬与复杂学习产品
  不匹配的规则。方向 A“证据工作台”作为全局骨架，方向 B 的专注布局和方向 C 的紧凑
  状态列表只作为同一设计系统内的局部页面模式；详细方案见
  `docs/design/milestone-7-frontend-redesign.md`。
- 第七里程碑 7C 边界：默认“今日”只聚合现有 API 能确定的当前任务、完成标准、到期复习、
  学习阻断和最近通知，不得推断不存在的数据或把流程完成表示为掌握。一级收件箱读取真实
  站内通知和未读状态，标记已读不代表关联事项完成；分类优先使用受管 severity/category，
  未知类别必须明确标为未分类。学习页不得重复展示通知列表，通知偏好继续保留在设置。
  只有学习提交实际创建对应能力范围证据时才生成 `evidence_update` 通知，失败或零证据提交
  不得生成；通知必须说明它不表示整门技能掌握。7C 不修改数据库、API 契约、技能包、依赖、
  第六里程碑工作区、服务器或部署状态。
- 第七里程碑 7D 边界：学习工作区必须以当前任务、完成标准、提交与结果为首要层级，规划、
  来源、版本锁和运行边界作为可追溯依据保留。独立证据中心只读取现有 API 返回的真实记录；
  在没有学习执行时不得把“无记录”表示为“无能力”。现有 API 不提供全历史学习执行列表，
  因此首版只展示精确技能版本的最新学习执行并明确该限制，不得伪造全历史聚合。六维等级、
  证据数量和人工／保持／来源待办必须分开显示；5C 外部真人评审只能作为独立补充记录，
  不得静默并入或抬高学习执行的六维等级。7D 保持零新增依赖，不修改数据库、API 契约、
  技能包、证据计算、第六里程碑工作区、服务器或部署状态，也不实施 7E 全站设计系统迁移
  或 7F 全站验收。7D 实现已通过完整本地 `release-readiness`：108 项后端测试通过、1 项
  按预期跳过，40 项前端测试、生产构建、6 项桌面／移动 E2E、契约无漂移和密钥扫描成功。
- 第七里程碑 7E 边界：使用仓库原生 CSS、React 和 HTML 语义建立设计令牌与共享反馈组件，
  统一色彩、排版、间距、控件、焦点、状态、空态、加载、表格和减少动效基线。状态不得仅靠
  颜色表达；错误和紧急警告必须使用正确 live region，表单说明与控件程序化关联。7E 保持
  零新增依赖，不修改后端、API 契约、技能包、证据计算、第六里程碑工作区、服务器或部署
  状态，也不实施 7F 全站体验验收。7E 已通过完整本地 `release-readiness`：108 项后端测试
  通过、1 项按预期跳过，42 项前端测试、生产构建与 13 个静态页面生成、桌面／移动共 8 项
  E2E、契约无漂移和密钥扫描成功。
- 当前未决定：
  - OpenAI、Moonshot AI 及 5B 范围外用途的首批模型清单；
  - 6B 后续运维复核、月度预算周期开始后的实际消耗；
  - 6D 观察期的最终起止时间，以及上线后账单实际消耗复核；
  - Logo、商标、公开品牌资产和公开发布计划；
  - 7F 的实施授权、外部设计技能是否在未来安装使用，以及 7E 之后是否新增前端依赖。

不得将未决定事项擅自变成既定方案。

## 3. 与用户协作的硬性规则

- 不得猜测用户意图、缺失需求、预算、技术偏好或产品优先级。
- 当缺失信息会实质影响产品行为、架构、成本、隐私、安全或后续返工时，必须停止相关实施，并向用户提出具体、容易回答的问题。
- 提问时说明：
  1. 哪一点尚不明确；
  2. 它会影响什么；
  3. 可选择的方案及主要差异；
  4. 推荐方案和推荐理由，但最终决定由用户作出。
- 必须清楚区分已确认事实、基于证据的推断、建议和待确认事项。
- 遇到用户陈述、项目文档、代码实现、Git 状态或验证证据之间存在分歧时，必须先停止会改变仓库或运行环境的操作，向项目所有者说明各方证据、时间、影响、可选处理方式和推荐方案，并询问项目所有者；在得到答复前不得自行选择一种解释、修改代码、终止进程、运行可能写入状态的验证、提交或推送。
- 不得以“行业惯例”代替用户确认关键产品决策。
- 不得擅自扩大任务范围、增加主要功能或进行无关重构。
- 可以执行范围明确、可逆且不会引入产品决策的常规实现步骤，但必须报告结果。

## 4. 知识来源与内容可信度

学习内容必须可追溯，不能仅凭模型记忆生成。

- 优先级从高到低通常为：
  1. 官方标准、规范、政府或监管机构资料；
  2. 官方文档和原作者资料；
  3. 同行评审论文、权威教材和专业组织资料；
  4. 高信誉机构、大学和公认专家资料；
  5. 其他来源只能作为补充，不得成为关键结论的唯一依据。
- 对可能变化的信息，必须记录来源、发布日期或版本、检索日期。
- 重要结论应尽量由两个相互独立的高质量来源交叉验证。
- 来源相互冲突时，不得静默选择其中一个；必须呈现冲突、解释可信度判断，并在必要时请用户决定。
- 每个学习单元应能够关联原始来源、证据强度和最近复核时间。
- 引用或整合内容时必须尊重版权、许可、访问条款和合理使用边界；默认保存摘要、结构化知识和出处，而不是未经许可复制全文。
- 不得伪造引用、作者、论文、链接、版本、市场数据或验证结果。
- 无法验证的信息必须明确标注为“未验证”，不能包装成事实。

## 5. 掌握程度规则

系统必须使用可观察证据评估掌握程度，至少考虑：

- 知识理解：能解释关键概念及其联系；
- 操作能力：能独立完成代表性任务；
- 迁移能力：能在陌生情境中应用技能；
- 作品证据：存在可检查的真实项目或成果；
- 保持程度：经过间隔复习后仍能完成测试或任务；
- 纠错能力：能发现问题、解释原因并完成修正。

不得因为用户完成课程、观看内容或通过单次简单测验，就宣称其“完全掌握”。

“掌握”必须针对明确的能力范围和评估标准，而不是对整门技能作无限范围的绝对声明。

## 6. 变现建议规则

- 就业、接单和产品化建议必须基于用户实际能力证据、作品、时间条件和可验证的市场信息。
- AI 可以比较三条路径并给出推荐，但必须展示判断依据、风险、前置条件和不确定性。
- 不得保证工作机会、订单、收入金额或投资回报。
- 市场价格、平台规则、岗位需求和法律税务信息属于易变化或高风险信息，使用前必须检索最新权威来源。
- 在没有足够掌握证据时，不得为了“尽快变现”建议用户提供超出其能力范围的服务。

## 7. 产品设计原则

- 学习路径围绕“诊断 → 学习 → 练习 → 项目 → 评估 → 复习 → 变现实验”形成闭环。
- 诊断阶段采用自适应访谈：AI 根据已知信息选择下一个最有价值的问题，并结合必要的知识测试或编程任务判断真实水平。
- 自适应访谈必须显示提问原因，允许用户跳过、不确定或纠正答案；不得把未经验证的回答永久当成事实。
- 当新增回答会显著改变计划时，AI 应更新受影响部分并保留变化说明，而不是无提示地重写整个计划。
- 每个阶段必须有明确目标、完成标准和可检查证据。
- 优先建立最小但完整的闭环，再扩展技能数量或附加功能。
- AI 输出应解释为什么安排某项内容，以及它与最终能力和变现目标的关系。
- 用户应能查看、修改或否决 AI 生成的学习计划。
- 所有自动更新应保留变更记录，避免知识内容在无提示的情况下发生不可追踪的变化。

## 8. 架构和开发约束

- 在用户确认技术栈之前，不得初始化框架、选择数据库、购买服务、创建云资源或添加基础性依赖。
- 作出架构建议前，先说明需求、备选方案、成本、隐私影响、锁定风险和推荐理由。
- 秘钥、令牌和个人敏感信息不得写入代码或提交到仓库。
- 外部 AI 请求必须经过统一的隐私过滤和发送开关；记录发送目的、数据类别、模型供应商和时间，但日志不得保存秘钥或不必要的原始敏感内容。
- 外部内容、AI 输出和用户输入都必须视为不可信输入，并在相应边界进行验证。
- 涉及外部来源抓取时，必须考虑访问条款、robots 规则、速率限制、版权和内容删除机制。
- 数据模型必须支持来源追踪、版本记录、证据记录和评估历史。
- 仓库内置技能包注册表与文件系统必须严格一致；缺失、未注册、校验和不符、依赖冲突或循环依赖都必须阻止应用启动和 CI 通过。
- 未来的无效远程第三方技能包不得拖垮核心服务，必须进入只读隔离区。系统应通知用户，并向当前由用户选择的 AI 提交经过安全净化的隔离审查材料，由 AI 给出修复、拒绝或后续教学安排建议；AI 不得执行隔离包、绕过重新验证或自行激活。
- 技能包生命周期必须支持与状态独立的 `availability: suspended`，用于安全、法律或数据完整性事件中的紧急停用，并保留完整审计记录。
- `deprecated` 技能包版本必须继续服务所有引用它的进行中计划，直到这些计划完成。新版内容只能通过单独版本锁定的补充学习单元或显式迁移加入，不能改写原计划的锁文件；用户必须收到变更说明。
- 新版补充学习单元必须先由 AI 生成差异、必要性、工作量和插入位置说明，得到用户确认后才能加入学习计划。用户之后可以撤销补充单元并要求 AI 重新规划；撤销和重规划都必须保留审计记录。
- 学习计划必须锁定根技能包、全部传递技能依赖、运行环境摘要、引擎协议和 Runner 协议的精确版本。
- FastAPI 进程不得直接导入或执行技能包中的评分器、自定义校验脚本或用户代码。所有可执行内容必须通过版本化、资源受限的隔离 Runner 协议调用。
- 仓库采用 pnpm workspace 管理 JavaScript 项目；根目录只允许存在 `pnpm-lock.yaml`，不得提交 `package-lock.json`、`yarn.lock` 或 `bun.lock`。
- FastAPI 应用位于 `apps/api`，Next.js 应用位于 `apps/web`；跨边界协议位于 `contracts`，不得从前端直接导入后端内部模块或反向导入。
- SQLite 数据文件、测试数据库、缓存、虚拟环境和构建产物不得提交。数据库访问必须集中在后端边界，前端不得直接读取 SQLite 文件。
- 后端数据访问采用 SQLAlchemy 2，数据库迁移采用 Alembic。应用启动不得使用 `create_all` 绕过迁移历史；Schema 变更必须通过版本化迁移完成。
- API 契约以 FastAPI 生成的 OpenAPI 文档为事实来源。生成后的契约制品必须提交，并通过无漂移检查确保代码与制品一致。
- 根目录脚本是本地与 CI 的统一入口。新增 CI 检查时，必须先提供可在本地运行的等价命令。
- 依赖安装和质量检查必须使用锁定模式：前端使用 `pnpm install --frozen-lockfile`，后端使用 `uv sync --locked`；发现锁文件漂移时直接失败。
- 任何技能包、合同或注册表校验失败时，FastAPI 不得以“部分可用”方式绕过仓库内置内容错误启动。
- 修改代码前先检查现有结构和约束；保持变更范围最小且目的明确。
- 不得声称构建、测试、检查或部署成功，除非实际执行并取得成功结果。

## 9. 验证要求

任何实现都应根据风险进行相称验证：

- 核心学习路径和评估逻辑必须有自动化测试；
- 来源追踪、版本更新和冲突处理必须有测试；
- 关键用户流程应有端到端验证；
- 涉及界面时应检查不同屏幕尺寸、空状态、错误状态、加载状态和基本无障碍；
- 无法执行的验证必须在交付说明中明确列出，不能省略不报。

## 10. 工作流程

对非简单任务按以下顺序进行：

1. 检查仓库和现有约束；
2. 列出已知条件、未知条件和风险；
3. 对会影响方向的未知条件先向用户提问；
4. 提交范围明确的实施方案；
5. 获得必要确认后实施；
6. 执行验证；
7. 报告完成内容、验证结果、遗留风险和下一项待确认决定。

### 10.1 项目接管与只读阅读规则

当项目所有者要求先阅读、接管或盘点项目，而没有同时授权继续实施时，默认只做只读检查。不得因此运行会创建数据库、缓存、构建产物或测试状态的命令，也不得终止服务、清理文件、修改代码、提交或推送。

为避免重复本项目在 2026-07-28 接管阅读中出现的无效尝试，后续代理应按以下方式检查：

- 先使用 `git status --short --branch`、`git diff --stat`、`git diff --name-status` 和 `git ls-files --others --exclude-standard` 确认真实变更范围。不要先递归枚举整个仓库，否则 `.pnpm-store`、`node_modules`、`.next`、`.venv` 和各类缓存会制造大量噪声并导致输出截断。
- 文本或文件搜索优先尝试 `rg`；如果当前 Windows 环境返回“拒绝访问”，记录一次失败后立即改用限定目录的 `Get-ChildItem` 与 `Select-String`，不要反复尝试同一不可用命令。
- 大文件和大范围 diff 应按文件或行号区间分批读取。并行读取结果发生截断时，不能把截断内容当作已完整审阅，必须改为读取相关文件的关键区段。
- 如果 `Get-CimInstance Win32_Process` 因权限不足失败，使用 `Get-Process` 核对进程路径和启动时间，使用 `netstat -ano` 或 `Get-NetTCPConnection` 核对监听端口；必要时只读访问本地 `/openapi.json` 比较接口版本。不得为了确认版本调用会写入数据库或触发外部请求的接口。
- 进程 ID 和端口属于易变化现场信息。任何后续终止操作前必须重新核对进程路径、启动时间、监听端口和项目关联，不能直接使用历史记录中的 PID。
- `.pytest_cache`、Playwright completion 文件、`test-results`、构建目录和日志只能证明某个时间点留下的结果，不能代替当前代码的重新验证。报告时必须标注其时间和“历史证据”属性；只有实际重新执行并成功，才能声明当前检查通过。
- 根目录 `.tmp` 被忽略，干净 Linux CI 不会预先存在该目录。统一 pytest 入口必须先显式
  创建临时目录父级，再把每次唯一子目录传给 `--basetemp`；不得依赖开发机历史目录掩盖
  干净检出失败。修改 pytest 启动器时必须用“不存在的模拟仓库根目录”测试首次创建。
- 用户交接说明与磁盘证据不一致时，必须按照第 3 节规则先询问项目所有者，不能静默采用任一方。

### 10.2 2026-07-28 端到端验证接管记录

以下内容是当日只读盘点结论，用于避免下一位代理重复排查；它不是永久状态，继续操作前必须重新核对：

- 第三里程碑已有 24 个已跟踪文件修改和 15 个未跟踪新文件，尚未提交；`main` 与 `origin/main` 当时均位于 `a49c665`。
- 后端 pytest 缓存列出 10 项测试且 `lastfailed` 为空；这只是历史证据。前端 lint、类型、测试和构建通过来自交接说明，未在本次只读盘点中重新执行。
- 旧 API 曾监听固定端口 `8000`，其 OpenAPI 不包含第三里程碑接口；动态端口版 API 曾监听 `33747`，其 OpenAPI 包含新接口。固定端口服务与测试服务混用确实能够造成新接口 HTTP 404。
- 动态端口修改已经存在于 `tools/run_e2e.mjs` 和 `apps/web/playwright.config.ts`，但当时尚未取得成功的 E2E 结果。最后一个可见 Playwright completion 文件在 `2026-07-28 00:40:58` 记录为 `failed`，桌面端和移动端均未显示规划标题。
- E2E 测试使用即时 `isVisible()` 决定是否点击“生成本地规划预览”，可能在页面仍处于加载状态时跳过点击。测试期望标题为“算法共同主干入门规划预览”，技能包模板实际标题为“算法共同主干入口规划预览”；即使端口问题解决，这一断言也需要统一。
- Windows 上的 E2E 进程清理当时没有可靠回收固定端口和动态端口 Python 进程，因此 `tools/run_e2e.mjs` 的停止逻辑仍需验证。
- 完整 `release-readiness` 当时没有通过。README 中“受限预览已完成”只能表示实现范围，不能表示发布就绪；在取得新鲜的完整成功结果前，应明确写成“实现完成、验证未完成”或等价表述。

建议处理顺序是：在获得项目所有者确认后重新识别并终止仅属于本项目的残留服务；修正 E2E 等待逻辑、标题断言和 Windows 清理逻辑；先运行 `check:e2e`；成功后运行完整 `release-readiness`；根据真实结果更新进度文档；最后再由项目所有者决定是否提交和推送。不得跳过确认直接执行这一顺序。

后续解决记录（2026-07-28）：项目仍保持 Node 24 LTS 基线。Codex PowerShell 的裸 `node` 曾从系统 PATH 解析为已经 EOL 的 Node 25，而 Codex 内置 pnpm 自身使用 Node 24，造成混合运行时；后续验证通过在单次命令中把 Codex 内置 Node 24 放到 PATH 最前解决。当前沙箱会拒绝 `taskkill`，E2E 启动器已在其失败时使用 Node `process.kill()` 终止真实监听 PID，并验证动态端口释放。修正等待逻辑、标题断言、缺失类型导入和测试明文凭据后，`check:e2e` 正常退出且桌面/移动测试通过，完整本地 `release-readiness` 也已通过。该结论不等于第三里程碑远程 GitHub Actions 已通过；推送后的远程 CI 仍需单独验证。

### 10.3 实时更新待办事项

根目录 `TODO.md` 是仓库级未完成事项的唯一账本。所有代理必须遵守：

- 开始非简单实施前先读取 `TODO.md`，核对当前门禁、阻塞、范围和下一步；
- 在任务范围内发现新的必要工作时，立即登记编号、状态、事项、前置条件或完成标准，不得只留在对话或临时笔记中；
- 待办状态、下一步、阻塞原因或验证证据发生变化时，必须在同一工作任务内同步更新，不得等到后续代理补记；
- 只有实际实现并完成相称验证后才能标记 `已完成`，同时记录可复核的命令结果、制品、提交或文档证据；历史缓存和交接陈述不能单独作为完成证据；
- `阻塞` 必须写明真实外部依赖、用户决定或失败证据；解除阻塞后应立即调整状态；
- 事项被取消、移出范围或替代时，不得静默删除，应保留 `已取消` 状态、原因和日期；
- 每次修改待办时更新文件顶部日期，并在维护记录中追加简短变更说明；
- 每一项任务结束前必须主动完成文档同步检查，并把它作为完成标准的一部分：至少更新
  `TODO.md` 中对应事项的状态、下一步、阻塞或验证证据并追加维护记录；当任务改变当前
  能力、里程碑状态、运行方式、用户可见入口或主要约束时，必须同时更新 `README.md`；
  当任务改变产品决定、架构、安全、隐私、成本、运行边界或操作流程时，还必须更新对应的
  `AGENTS.md`、`docs/architecture/`、`docs/design/`、`docs/operations/` 或证据文档。不得
  以“代码已经说明”为由省略文档，也不得把同步工作推迟给后续对话；
- 如果一项任务经核对确实没有改变任何持久状态、决定、待办或验证证据，例如只读回答，
  可以不制造无意义的 README 改动，但必须在任务交付说明中明确写明“文档无需更新”及理由。
  任何代码、配置、部署、数据、依赖、设计决定或可复核验证发生变化时，不适用该例外；
- `TODO.md` 只记录和追踪工作，不构成实施、提交、推送、部署、购买或扩大范围的授权；
- `TODO.md` 与用户指令、项目文档、代码、Git 状态或验证证据不一致时，必须按第 3 节停止相关实施并询问项目所有者。

首个开发里程碑必须按“最小项目骨架 → 本地确定性检查 → CI 基线 → 业务功能”的顺序执行。CI 基线未通过前，不得开始实现自适应访谈、学习规划等业务功能。

本地与 CI 的首批稳定检查入口至少包括：

- `check:markdown`：Markdown 基线质量；
- `check:structure`：仓库结构、受管文件和禁止锁文件；
- `check:registry`：注册表、manifest、内容完整性和依赖图；
- `check:contracts`：JSON Schema 与 OpenAPI 契约；
- `check:api`：FastAPI lint、格式、类型和测试；
- `check:web`：Next.js lint、类型、测试和构建；
- `check:contract-drift`：重新生成契约后不得出现差异；
- `check:secrets`：仓库敏感信息基线扫描；
- `release-readiness`：汇总所有适用检查。

所有检查必须有真实校验对象，不能使用永远成功的占位命令。工作流不得使用路径过滤跳过必需检查；即使某一部分没有变化，稳定检查名称也必须产生明确结果。

### 10.4 Codex Windows 本地服务启动规则

2026-07-30 的真实供应商烟雾验证准备中，Codex 连续出现“启动命令长时间不返回”的问题。
后续代理必须遵守以下结论和操作边界，不能再次用内联后台进程命令试错：

- 当前 Codex PowerShell 环境可能同时包含大小写不同的 `Path` 和 `PATH`。在该环境中，
  `Start-Process` 会在构造环境字典时抛出重复键异常；`-UseNewEnvironment` 也不能可靠
  规避。不要在默认沙箱 PowerShell 中反复尝试 `Start-Process`。
- Codex 沙箱执行器会继续等待它创建的长生命周期后代进程。即使 Node 使用
  `detached: true`、`unref()`、隐藏窗口并把标准输入输出分离到文件，直接通过普通
  `shell_command` 启动 API/Web 仍可能让工具调用一直不结束。不得把“服务已健康但工具
  仍等待”误判为 API 或 Next.js 启动失败。
- 仓库统一入口为 `local-services:start`、`local-services:status` 和
  `local-services:stop`，实现位于 `tools/run_local_services.mjs`。启动器固定要求
  Node 24，直接调用后端虚拟环境和 Next.js CLI，规范化子进程环境，使用
  `.tmp/local-services` 中的状态、PID 和日志文件，并对就绪检查设置 45 秒硬超时。
- Windows 虚拟环境的 `python.exe` 可能是转发启动器，其 PID 不等于实际 Uvicorn PID。
  `tools/run_local_api.py` 会由实际 Python 进程写入 PID；停止或诊断时应以状态文件中的
  实际 PID、进程路径、启动时间和监听端口为准，不能只跟踪最初的启动器 PID。
- 启动器健康探测必须使用会明确关闭连接的请求，不能依赖可能保留事件循环句柄的默认
  HTTP 客户端；也不能为强制结束而在异步句柄关闭期间直接调用 `process.exit()`。2026-07-30
  曾因此触发 Windows libuv `UV_HANDLE_CLOSING` 断言，虽然服务已启动，但启动器本身不算
  验证成功。
- 普通用户 PowerShell 可以直接运行仓库入口。Codex 不得启动需要跨工具调用或跨轮次持续
  存活的本地服务：普通沙箱会等待长期后代进程，经批准的沙箱外隐藏 `Start-Process`
  虽然可以短暂通过健康检查，但 Codex 作业结束后仍会回收其后代进程，不能作为持久启动
  方案。需要交互使用本地页面时，应由项目所有者在普通 PowerShell 中使用精确 Node 24
  执行 `tools/run_local_services.mjs start`；`status` 和 `stop` 是有界命令，Codex 可以
  在普通沙箱内执行。若以后希望无需用户终端即可常驻，必须另行确认 Windows 计划任务、
  服务或其他持久进程管理方案及其系统状态影响。
- 启动后使用 `local-services:status`、`netstat -ano`、本地 `/health` 和设置页交叉验证。
  单一端口命令返回空结果时不能立即下结论；本项目曾出现 `Get-NetTCPConnection` 未显示
  监听但 `netstat` 与 HTTP 200 均证明服务健康的情况。
- `check:e2e` 必须通过 `CLOUD_STUDY_E2E_NEXT_DIST_DIR` 使用仅位于 `apps/web` 下、名称为
  `.next-e2e` 的独立临时构建目录，并在启动前及停止自身子进程后校验目标再清理。该固定
  目录的类型路径必须预先列入 `tsconfig.json`，不得让 Next 每次写入随机路径。不得复用
  正在运行的本地开发服务所持有的 `.next`，也不得为了取得 E2E 锁而终止用户服务。
- Next 开发服务器会重写受跟踪的 `apps/web/next-env.d.ts`。E2E 启动器必须在启动前保存
  该文件的原始字节，并在正常结束、失败、信号和进程退出路径恢复；不得把临时
  `.next-e2e` 引用提交为普通开发基线。提交前应比较运行前后哈希，不能只依赖测试通过。
- 启动应用会运行 Alembic 并写入本地 SQLite，必须已有相应实施或运行授权。若启动因
  技能包内容锁不一致失败，不得删除数据库、改写锁或静默切换数据库。必须只读记录锁定
  摘要、当前摘要、引用数量和创建时间，并由项目所有者判断数据是否为可放弃的测试数据。
  即使确认是测试数据，也应先把原数据库移动到 `.tmp/database-backups` 留下可恢复备份，
  再创建新活动数据库。
- 任何启动调用不得无反馈等待超过 60 秒。若 45 秒内没有明确结果，立即读取状态文件、
  日志、PID、端口和 HTTP 健康状态；在确认项目关联前不得终止进程。真实来源访问和付费
  模型调用必须继续遵守各自的逐次确认，服务启动授权不能代替外部调用授权。

### 10.5 DeepSeek 价格核验与预派发恢复规则

2026-07-30 首次真实 DeepSeek 综合准备中，官方价格与锁定策略完全一致，但官方价格表使用
`rowspan` 增加“价格”分组列，本地解析器按固定绝对列号读取，错误触发
`pricing_changed_or_unverifiable`。后续代理必须遵守：

- 价格解析必须先在表头识别 `deepseek-v4-flash` 的相对模型位置，再逐行定位“缓存命中”、
  “缓存未命中”和“百万tokens输出”等价格标签，从标签相邻的模型价格列读取；不得假设各行
  使用相同绝对列号，也不得用其他模型或历史价格填补。
- 真实页面验证可以不带 API 密钥只读访问官方价格页，并必须同时核对 HTTP 状态、内容类型、
  三项价格和精确模型名；该验证不等于模型调用成功。
- `pricing_changed_or_unverifiable` 默认仍是停止条件。只有项目所有者明确确认恢复，并且运行
  没有 synthesis attempt、dispatch、响应、外发同意、token、费用或计费时间证据，且仍有
  至少两个可外发官方来源时，才能恢复到 `synthesis_pending`。
- 恢复必须追加保留原失败码的审计事件，不能删除或改写原失败事件。恢复动作本身不得调用
  DeepSeek；恢复后仍须重新取得逐次外部 AI 与最高费用确认。任何已 dispatch 或收费状态
  未知的失败都不得使用该恢复路径，也不得自动重试。
- 2026-07-30 首次真实模型调用取得 HTTP 200 和精确模型标识，但综合内容未通过 JSON／
  业务结构校验。该运行只保留响应哈希并以 `deepseek_invalid_response` 终止，本地按响应
  使用量保守记账；不得为了判断原始响应形态而再次付费调用。未来若实施诊断，只能新增不
  保存原始正文的失败阶段、顶层结构、校验类别和 usage/费用元数据；再次调用仍须逐次确认。
- 后续已采用 `limited_background_v1` 受限响应协议：模型只能返回逐路径背景摘要、所引用
  的本次来源 ID、不确定性和整体限制，禁止模型返回路径状态、内容影响建议、收入判断或
  对外行动。只要受管目录仍为 `context_only`，后端必须确定性生成三条路径的
  `indeterminate` 状态，并把内容影响固定为 `no_change`；不得信任或转存模型自报状态。
- 无效响应诊断只允许保存协议版本、失败阶段、受管顶层键识别结果、缺失键、未知键数量、
  校验类别、内容长度与哈希（仅 JSON 解析失败时）以及已知 usage/费用。不得保存无效响应
  原文、未知键名或未知值。即使响应内容无效，只要 usage 可验证，就必须保存实际 token 与
  计算费用，不能把已发生调用记为零费用；不得恢复或重试已经 dispatch 的失败运行。

### 10.6 Docker／WSL Runner 安装、长任务与只读容器规则

2026-08-01 的 4B 实机安装和真实验证暴露了 Windows 检测、长镜像拉取、磁盘计量及
Docker 只读容器数据通道的差异。后续代理必须遵守：

- 固件虚拟化不能只读 `Win32_Processor.VirtualizationFirmwareEnabled`。管理程序已接管
  时该字段可能为 `False`；必须交叉核对用户当前任务管理器、`Win32_ComputerSystem.
  HypervisorPresent`、VBS 和 `systeminfo`。处理器字段为真或管理程序已存在，且没有
  相反的当前证据时，才可通过安装脚本的虚拟化门禁；证据冲突时按第 3 节停止并询问。
- Windows PowerShell 5 在 `$ErrorActionPreference = "Stop"` 下可能把 WSL 尚未安装时的
  原生 stderr 提前转成终止错误。所有需要按退出码分支的原生命令必须在最小局部范围把
  错误偏好降为 `Continue`、读取 `$LASTEXITCODE` 后恢复，不能让预期的“未安装”状态跳过
  后续安装逻辑。
- UAC 只用于确需管理员权限的 WSL／Docker 安装步骤。脚本应先用 WindowsPrincipal
  明确验证管理员令牌，并把日志写入 `D:\CloudStudy\Logs`；仓库脚本被本机执行策略
  阻止时，只允许对该次受管进程使用 `powershell.exe -NoProfile -ExecutionPolicy Bypass
  -File ...`，不得持久修改机器或用户执行策略。
- 个人版 Docker Desktop 的 CLI 和 `docker-credential-desktop` 位于用户目录，当前
  Codex 进程可能尚未继承安装后的 PATH。脚本必须解析已确认的个人版安装目录，并只在
  自身进程前置该目录；不得因此改写系统 PATH，也不得把裸 `docker` 不可见误报为未安装。
- 首次拉取两项精确摘要镜像约有 3.784 GB 唯一镜像内容，本机实测约 20 分钟。开始前
  必须先用精确摘要执行 `docker image inspect`，已存在则跳过；缺失时显示当前阶段并设置
  30 分钟硬超时。只要 Docker 层进度、守护进程或摘要检查仍有进展，不得把工具等待误判
  为死锁。学习者练习继续强制 `--pull never`，不得在练习路径补拉镜像。
- 磁盘门禁必须保守测量 `D:\CloudStudy\DockerData` 的实际文件／VHDX 占用，不能只引用
  `docker system df -v` 的镜像内容。本机镜像唯一内容为 3.784 GB，但非稀疏
  `docker_data.vhdx` 实占曾为 5.137 GB；项目所有者已将数据根硬预算确认调整为 6 GB，
  启动安装前至少 8 GB 可用的门槛不变。超过 6 GB 时必须停止，不得自动 prune、删除
  镜像、改换摘要或扩大预算。
- 标记为 `--read-only` 的容器会拒绝 `docker cp` 写入，Docker 守护进程的复制视图也
  不能可靠读取运行时 tmpfs。Runner 必须先启动只读、断网、非 root 的受管容器，再通过
  `docker exec -i tar -x` 把内存归档流入容量受限的 `/tmp`；编译产物通过容器内
  `tar -c` 流式取回。只有承载待执行文件的 `/tmp` 可显式使用 `exec,nosuid,nodev`，
  根文件系统仍只读，且不得改为宿主目录、Docker Socket、数据库、凭据、仓库或用户路径
  挂载。
- 真实结论必须来自当前的精确镜像检查、数据根计量、`pnpm check:runner-live` 结果和
  项目标签残留检查。工具超时、历史缓存或一次失败的中间状态都不能单独作为“卡死”或
  “验证通过”的证据；异常清理仍只能处理带 `cloud-study.runner=1.1.0` 标签的容器。

## 11. 规则来源

本文件为本项目专门编写，结构参考了以下公开 GitHub 项目：

- AGENTS.md 开放格式：<https://github.com/agentsmd/agents.md>
- “需求不清时停止并提问、不得猜测”的变更安全原则：
  <https://github.com/telemt/telemt/blob/main/AGENTS.md>

这些来源只用于形成通用协作和工程约束；本文件未沿用其项目特定的技术栈、命令或架构决定。
