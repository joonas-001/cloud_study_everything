# 云奕学项目状态

> 状态更新时间：2026-08-21（Asia/Shanghai）。本文件记录当前证据、边界和下一步，
> 不替代 `AGENTS.md` 中的项目所有者指令。

## 当前结论

- 6A 已交付，6B 合成数据私有预发布已在现有新加坡单实例完成当前范围验收；
- 6C 真实数据库加密副本迁移与回滚演练已通过，唯一真实库未替换、未迁入云端；
- 远程 Runner 精确候选 `f1d78f8` 已通过云端十项真实安全与资源矩阵；
- 6D 已按授权范围完成受控激活，当前状态为“已激活、观察中”；生产策略为 `1.1.0`，
  远程 Runner 已通过独立 Broker 接入 live API；
- 6C 已通过 PR #16 合并到 `main@65529de`，PR 与合并后主线 CI 均为 22 项成功；
- 6D 受控启用策略、持久 Broker、完整矩阵和失败回退已在独立分支实现；
- 8 个抽样跟踪文件的 Git blob 哈希已确认云端当前应用为 7F 提交 `6acdaca`；项目所有者
  已确认采用独立 6D 基线，接受正式部署后界面回到当前主线基线；
- 6D 当前完整本地 `release-readiness` 已通过：115 项后端测试通过、1 项按预期跳过，
  42 项前端、生产构建、桌面／移动 8 项 E2E、契约无漂移和密钥扫描成功；
- 精确提交 `5b4af16` 已部署；Draft PR #19 最新 GitHub Actions 运行 `32445151440`
  共 22 项检查全部成功；
- 真实库 47 张表／54 行迁移一致，最终本地备份已真实恢复，首份云端策略 `1.1.0` 加密备份
  已生成；旧 app、旧合成库、环境文件和本地库均保留回滚点；
- Runner 激活和独立复测十项矩阵均通过，项目容器残留为 0；身份、Tailnet HTTPS、四条
  关键路由、服务、Socket 权限、端口和资源余量通过；
- Headless Chromium/Edge 的远端自动烟雾因 `ERR_CONNECTION_CLOSED` 未通过；真实浏览器
  交互、观察期时长和腾讯云账单实耗仍待项目所有者确认；
- 真实 AI、真实来源、邮件、公开发布、扩容和新增付费资源仍未授权。

## 6C 证据

| 范围 | 状态 | 证据或限制 |
| --- | --- | --- |
| 真实数据演练 | 通过 | 47 张表、54 行；Schema、逐表语义摘要、外键、事件顺序、内容锁和反向恢复一致 |
| 本地服务恢复 | 通过 | 写入冻结后原 API/Web 已恢复健康 |
| 临时明文副本 | 未保留 | 只保留本机安全目录中的加密制品和无正文报告 |
| Runner 代码门禁 | 通过 | 最终 `release-readiness`：114 项后端通过、1 项按预期跳过，42 项前端、生产构建、8 项 E2E、契约与密钥扫描成功 |
| 本机 Runner 复测 | 未执行 | Docker Desktop 未启动，探针在容器创建前停止；历史 4B 证据不受影响 |
| 云端 Runner 实测 | 通过 | Docker 29.1.3；锁定的 GCC 15.2.0 与 Python 3.14.3 镜像；Unix Socket 十项矩阵全部符合预期 |
| 云端权限与清理 | 通过 | FastAPI 身份不在 Docker 组且不能读取 Docker Socket；验证后项目容器为零，Broker 为 `inactive + static` |
| 云端资源与 live 回归 | 通过 | 根盘使用 31%，镜像 3.785 GB，可用内存 1.3 GiB、Swap 1.8 GiB；API、Web、备份计时器与 Tailnet HTTPS 正常 |

## 下一步

1. 由项目所有者确认观察期时长，并在真实桌面／移动浏览器中完成交互确认；
2. 在腾讯云账单侧复核当月实际消耗仍符合 ¥50 硬上限和 50%／80% 告警；
3. 观察期内保持本地库、旧 app、旧合成库和加密备份可回滚；结束时再决定接受上线或回滚。

完整 6D 证据见 `docs/operations/milestone-6d-validation.md`。
