# 云奕学当前待修复问题

> 审计日期：2026-07-29（Asia/Shanghai）
>
> 审计范围：项目初衷与项目级约束、里程碑文档、技能包、4A、5A、当前
> `codex/milestone-5b` 工作区中的 5B 后端、数据库迁移、API、前端、契约、
> 治理检查、自动化测试和 CI 配置。
>
> 文档性质：本文件用于详细解释审计发现，不是第二份进度账本。问题状态、实施授权、
> 阻塞关系和完成证据仍以根目录 `TODO.md` 为唯一事实来源。
>
> 当前状态：本文件是 2026-07-29 的历史 5B 审计，不代表当前工作区待办。
> `FIX-5B-001` 至 `FIX-5B-014` 已完成，PR #4 已合并到 `main@273c190`，合并后 CI
> `30524441148` 共 19 项成功；当前状态、6A 审计修复和后续门禁统一以 `TODO.md` 为准。
>
> 真实验证：2026-07-30 已访问国家统计局、工信部和 DeepSeek 官方价格页，并在项目
> 所有者逐次确认后调用一次 `deepseek-v4-flash`。响应 HTTP 200、模型匹配，但内容结构
> 校验失败，保守记账 ¥0.003597，未生成可采纳结论。随后实现
> `limited_background_v1` 与隐私安全分阶段诊断；修复后未再次付费调用，30 天间隔和
> 逐次确认不得绕过。

## 1. 结论摘要

项目整体方向没有失控。以下基础边界与项目初衷基本一致：

- 4A 没有把阅读完成、结构校验、自评或单次简单练习包装成“已经掌握”；
- 4A 不执行用户代码，不上传文件，不调用外部 AI；
- 5A 支持非变现目标，不强制用户进入就业、接单或产品化比较；
- 5A 的市场夹具显著标记为合成数据，不产生真实交付资格；
- 5B 锁定了 DeepSeek 官方地址、`deepseek-v4-flash`、预算上限、外发总开关、
  逐次确认、Windows 凭据引用、禁止自动充值和禁止静默切换模型；
- Node.js、Python、pnpm、uv、Alembic、OpenAPI、许可证及 4B/5C 独立门禁总体符合约束。

但是，当前 5B 的“来源变化识别、证据可靠性、通用性和付费调用恢复”仍存在阻断级问题。
这些问题会导致来源变化漏报、市场结论缺少真实支撑、第二个技能无法复用通用流程，或者
研究记录永久卡死。自动化测试通过不能消除这些产品与逻辑缺陷。

## 2. 阻断级问题

### 本次处理结果

- `FIX-5B-001`：已分离原始响应、完整规范化正文和短摘录哈希；模板变化记录为
  `metadata_changed`，摘录外正文变化记录为 `changed`。
- `FIX-5B-002`：已增加路径证据能力、`independence_group` 和本次正文相关性门禁。
  当前首版目录三条路径均明确为 `context_only`，只能返回不可确定判断。
- `FIX-5B-003`：已增加版本化市场研究注册表，并锁定技能、精确技能包版本、能力范围、
  当前用户目标和可选准备度评估；第二个测试技能无需修改核心服务。
- `FIX-5B-004`：已增加持久化 attempt、阶段审计、5 分钟租约、启动恢复、
  `recovery_required`、未知收费最坏记账和显式结束接口，恢复不会产生第二次模型调用。
- 验证：Node 24.14.0 下完整本地离线 `release-readiness` 成功，包括 53 项后端、
  12 项前端、生产构建、桌面/移动 E2E、契约无漂移和密钥扫描，费用 ¥0。

### FIX-5B-001：来源变化检测可能静默漏报

**问题现状**

`apps/api/src/cloud_study_api/market_research.py` 同时保存了：

- `raw_response_sha256`：完整 HTTP 响应的哈希；
- `content_sha256`：净化后、最多 2000 字符摘录的哈希。

但是，新旧来源比较使用的是 `content_sha256`，没有使用完整响应哈希。

**为什么这是错误**

只要网页前 2000 字符保持不变，后续正文即使发生重要变化，也可能被记录为
`unchanged`。这与以下项目要求直接冲突：

- 来源变化不得被静默忽略；
- 系统应持续追踪知识和市场变化；
- 变化记录必须可追溯；
- 5B 完成标准要求来源变化、失败和冲突形成可信闭环。

**可能影响**

- 新岗位、政策、统计数据或删除内容位于页面后部时无法发现；
- 用户会看到错误的“无变化”状态；
- 后续 AI 综合不会被触发；
- 历史审计会保存一个确定但错误的判断。

**建议修复方向**

1. 使用完整响应哈希作为原始内容变化的第一判断依据；
2. 另行保存规范化正文哈希，用于减少导航、时间戳等页面噪声；
3. 完整响应变化但规范化正文未变化时，记录明确的“元数据或模板变化”状态；
4. 不得仅以截断摘录哈希代表整页内容；
5. 为“前 2000 字符相同、后部变化”的场景增加自动化测试。

**完成标准**

- 页面任意位置的正文变化不会被错误标记为 `unchanged`；
- 原始哈希、规范化哈希、摘录哈希的用途在契约和文档中明确区分；
- 新增正反例测试并进入 `release-readiness`；
- 历史记录仍保留原始哈希和变化判定依据。

### FIX-5B-002：当前来源无法可靠覆盖就业、接单、产品化三条路径

**问题现状**

当前官方来源目录中：

- 国家统计局：三条路径的宏观背景；
- 人力资源和社会保障部统计入口：就业宏观背景；
- 中国公共招聘网首页：唯一的就业直接信号；
- 工业和信息化部数据入口：三条路径的宏观背景；
- 接单和产品化没有任何直接信号来源。

运行时验证要求 `supported` 或 `conflicted` 至少包含一个 `direct_signal`。
因此，接单和产品化在当前目录下无法得到这两种结论。

另一方面，中国公共招聘网被抓取的是网站首页，而不是针对“初级 C++ 后端”或
“算法应用工程”的岗位查询结果。代码只验证来源 ID、路径标签和
`evidence_role`，没有验证本次摘录是否真的出现目标岗位。

**为什么这是错误**

当前实现会同时产生两种相反风险：

1. 接单和产品化长期只能返回 `indeterminate`，与“首版覆盖三条路径”的决定不一致；
2. 就业首页即使没有目标岗位，也可能因为静态 `direct_signal` 标签而被模型引用，
   形成表面上满足规则、实际上没有直接岗位证据的结论。

此外，“两个独立来源”目前只按不同 `source_id` 计数，没有独立机构、数据所有者或
共同上游分组。同一机构的两个页面可以被错误视为两个相互独立的来源。

**可能影响**

- 市场研究结果看似结构化，但证据并不支持结论；
- 就业路径可能产生假阳性；
- 接单和产品化可能永久无法判断；
- 用户无法依据结果可靠比较三条路径；
- 与“重要结论尽量由两个独立高质量来源交叉验证”的规则冲突。

**建议修复方向**

1. 为每条路径定义可观察的直接信号和允许的官方获取方式；
2. 就业使用官方岗位查询结果、官方数据接口或可复核岗位记录，而不是网站首页；
3. 接单和产品化在没有合格直接来源时，明确保持
   `indeterminate`，不得声称已经覆盖；
4. 为来源增加 `independence_group`、数据所有者或共同上游标识；
5. 验证结论引用的本次摘录是否包含与目标范围相关的可观察信号；
6. 不能可靠验证的路径应在 UI 中展示“当前来源体系不支持判断”，而不是笼统显示
   “市场研究已覆盖”。

**完成标准**

- 三条路径的“已覆盖”含义与实际证据能力一致；
- `supported/conflicted` 的每个结论均引用本次成功取得且内容相关的证据；
- 两个来源的独立性不再仅由 ID 不同决定；
- 只有宏观背景时确定性返回 `indeterminate`；
- 测试覆盖无目标岗位、同机构双页面、路径错配、来源撤回及直接信号缺失。

### FIX-5B-003：5B 把算法硬编码进通用市场研究流程

**问题现状**

当前 5B 服务：

- 固定读取 `readiness/sources/official-cn-market-v1.json`；
- 固定读取 `deepseek-v4-flash-budget-v1.json`；
- 固定生成“算法技能相关市场信号”的任务描述；
- `market_research_runs` 没有保存 `skill_id`、精确技能包版本、
  `capability_scope_id`、用户目标选择或准备度评估引用；
- 页面显示全局最新研究，而不是当前技能、能力范围和目标对应的研究。

**为什么这是错误**

项目已经明确：

- 先完整做好算法，但底层必须是可复用的通用学习引擎；
- 新技能通过独立技能包扩展；
- 不得把首个技能硬编码进通用流程；
- 市场建议必须关联实际能力证据、技能范围和用户当前目标。

当前结构增加第二个技能时必须修改市场研究业务代码和数据库，而不能只增加新技能包或
版本化目录。

**可能影响**

- 第二个技能无法复用 5B；
- 一次算法研究可能被错误展示给其他技能或其他能力范围；
- 5A 的目标、证据与 5B 市场结果无法形成精确可追溯关系；
- 历史记录不能回答“这份研究针对哪个技能版本、能力范围和用户目标”。

**建议修复方向**

1. 建立版本化市场研究目录注册表；
2. 每次研究明确锁定 `skill_id`、技能包版本、能力范围、用户目标及市场范围版本；
3. 把“研究主题”和允许路径放入受管目录，而不是写死在服务提示中；
4. 页面按当前目标和能力范围选择或展示研究；
5. 保持 DeepSeek、预算、来源获取等为通用基础设施，算法只作为首个受管实例。

**完成标准**

- 增加第二个测试技能时不需要修改 5B 核心服务代码；
- 每次研究能够追溯到精确技能版本、能力范围、用户目标、来源目录和预算策略；
- 不允许把一项技能的研究用于另一项技能；
- API、OpenAPI、TypeScript 契约、迁移和 E2E 均覆盖这些锁定关系。

### FIX-5B-004：付费调用或进程异常可能永久卡死研究

**问题现状**

付费综合取得执行权后，研究进入 `synthesis_in_progress` 并预留最坏费用。
调用代码只明确捕获 `MarketResearchError`。DNS、TLS、底层 HTTP 异常、未预期异常、
进程崩溃或机器重启可能发生在状态写入之后。

当前没有：

- 执行租约或超时时间；
- 启动时恢复或对账；
- 用户取消入口；
- 管理员式本地恢复入口；
- 对未知异常的保守费用记账和失败审计；
- 区分“已发出但响应未知”和“尚未发出”的状态。

**为什么这是错误**

活动研究存在唯一约束。遗留的 `synthesis_in_progress` 会持续阻止新研究，并可能永久占用
预算。用户既不能继续，也不能安全判断是否已经产生费用。

**可能影响**

- 研究永久不可恢复；
- 后续来源检查和综合被阻断；
- 预算一直显示为已预留；
- 实际产生费用但没有审计结果；
- 用户可能尝试手工修改数据库，破坏审计完整性。

**建议修复方向**

1. 为付费调用增加不可变 attempt 记录和执行租约；
2. 明确记录 `claimed`、`dispatch_started`、`response_received`、`accounted` 等阶段；
3. 未知异常按最坏费用保守记账并进入明确终态；
4. API 启动时识别超过租约的遗留调用，进入需要用户复核的恢复状态；
5. 不自动重试，不自动再次付费；
6. 提供本地、显式确认的结束或对账流程；
7. 测试进程中断、DNS/TLS 异常、数据库提交失败和响应解析异常。

**完成标准**

- 不存在永久不可恢复的 `synthesis_in_progress`；
- 未知是否收费时不会记为零费用；
- 恢复过程不产生第二次模型调用；
- 活动研究唯一锁最终可以通过可审计流程释放；
- 并发、崩溃恢复和预算计算测试通过。

## 3. 重要问题

### 本次处理结果

- `FIX-5B-006`：后端预览和实际综合请求复用同一净化材料；界面展示发送与排除类别，
  并可逐项确认删除摘录、触发综合失效。
- `FIX-5B-007`：会改变状态的 API 失败后保留原始错误，并重新加载概览与审计历史；
  移动 E2E 验证无需手动刷新即可看到失败终态。
- `FIX-5B-008`：Alembic `0008` 分离保存请求与响应模型；缺失或不一致响应停止并按
  单次 ¥0.20 硬上限保守记账。
- `FIX-5B-009`：治理检查增加所有者独立组、路径声明和刷新策略一致性，关键保护由
  服务级正反例测试证明。
- `FIX-5B-010`：Playwright 使用独立离线 FastAPI 宿主；桌面覆盖成功、人工接受、
  删除和失效，移动覆盖模型不一致、失败状态刷新和保守记账。
- 验证：Node 24.14.0 下完整 `release-readiness` 成功，包括 57 项后端、13 项前端、
  生产构建、桌面/移动 E2E、契约无漂移和密钥扫描；费用及真实外部访问均为 0。
- `FIX-5B-005`：来源目录 `1.2.0` 已固定失败后冷却 24 小时、首版不允许人工绕过，
  成功刷新保持 7 天；逐来源访问与成功快照、API/UI 倒计时和审计均已实现。

### FIX-5B-005：失败或阻断后可以立即重复访问官方来源

**原问题现状**

7 天来源刷新间隔只参考 `status == completed` 的上一条记录。
`blocked` 和 `failed` 研究不参与间隔判断，且不属于活动研究。

**影响**

- 用户可在来源失败后立即反复访问全部官方站点；
- 网络或价格失败后也可能重复执行相同来源抓取；
- 与受控频率、站点条款和友好访问原则不一致；
- 可能制造大量内容相同但状态不同的历史记录。

**建议修复方向**

- 将“最近一次来源访问 attempt”和“最近一次成功来源快照”分开记录；
- 失败后采用明确、版本化且不静默重试的人工冷却规则；
- 允许用户查看失败原因，但不允许通过新建研究绕过冷却；
- 对按来源的最近访问时间、总体研究时间和手动例外分别测试。

**完成标准**

- 失败或阻断不能无限重复访问官方来源；
- 成功刷新间隔和失败冷却规则在 UI、契约及审计中清晰可见；
- 不把访问失败记录成“无变化”。

**修复结果**

- 逐来源分别保存 `access_attempted_at`、访问结果和 `successful_snapshot_at`，删除摘录
  不会抹去原访问证据；
- 成功来源按 7 天刷新间隔，失败来源按 24 小时冷却；没有人工例外参数或界面入口；
- 部分失败在 24 小时后只访问失败来源，仍在成功窗口内的来源复用不可变快照；
- 概览契约和界面展示逐来源最近访问、最近成功快照、下次允许时间及总体剩余时间；
- 正反例覆盖立即重试、23 小时 59 分、24 小时边界、部分失败复用、模型失败后的
  7 天门禁和审计字段，且离线 E2E 展示规则与无绕过边界。

### FIX-5B-006：外发确认前看不到实际材料，界面也不能执行摘录删除

**问题现状**

界面要求用户确认“发送净化后的最少官方摘录给 DeepSeek”，但确认前只显示可用来源数量，
没有逐项展示本次实际准备发送的来源 ID、摘录、限制和数据类别。

后端实现了摘录删除 API，但当前前端没有对应操作入口，也没有在 API 客户端封装该接口。

**影响**

- 用户不能充分了解本次外发内容；
- “逐次确认”形式存在，但知情程度不足；
- 用户无法通过正常产品界面执行已声明的内容删除策略；
- 删除能力只存在于 API 文档，不构成完整的用户闭环。

**建议修复方向**

1. 外发确认前显示最终净化材料预览；
2. 明确显示不会发送和将会发送的数据类别；
3. 按来源提供显式确认的摘录删除入口；
4. 删除后重新计算可用来源数并阻止失效综合；
5. 不在审计事件中保存完整外发材料或密钥。

**完成标准**

- 用户能够在发送前检查每项实际外发材料；
- 用户能够从界面删除摘录并看到综合失效结果；
- 桌面和移动 E2E 覆盖预览、删除、确认与取消。

### FIX-5B-007：后端失败后前端可能继续展示旧状态

**问题现状**

前端 `runAction` 只在请求成功时更新 `overview.latest_run` 和历史记录。
如果价格核验失败、超时或其他后端错误已经把研究改成 `failed`，前端只展示错误文本，
仍保留旧的 `synthesis_pending` 状态。

**影响**

- 页面可能继续显示已经失效的综合按钮；
- 用户再次点击后只会得到状态冲突；
- 费用、失败代码和审计状态不能及时显示；
- 用户需要手动刷新页面才能看到真实状态。

**建议修复方向**

- 对可能改变后端状态的失败请求，在错误处理后重新获取 overview 和 history；
- 区分“请求未到达后端”和“后端处理后返回失败”；
- 保留原始错误，同时刷新最新研究状态；
- 加入价格失败、超时、用量越界和综合格式错误的前端测试。

**完成标准**

- 后端状态变化后，前端不会继续显示旧操作入口；
- 错误信息、失败代码、预算计入和最新审计可以同时查看；
- 失败状态 E2E 通过。

### FIX-5B-008：没有校验 DeepSeek 响应中的实际模型标识

**问题现状**

请求固定发送 `deepseek-v4-flash`，研究也锁定该模型，但响应解析没有核对响应中的
`model` 字段。

**影响**

- 无法证明供应商返回记录与锁定模型标识一致；
- 供应商路由、模型别名或异常响应变化不能被明确发现；
- 审计中的模型只反映请求配置，不一定反映响应声明。

**建议修复方向**

- 验证响应 `model` 与锁定模型允许的精确标识规则；
- 同时保存请求模型和经过验证的响应模型；
- 不一致时进入失败或人工复核状态，不静默接受；
- 价格与模型校验必须使用同一锁定预算策略。

**完成标准**

- 模型不一致的响应不能进入人工接受流程；
- 测试覆盖缺失、错误和允许的响应模型标识。

### FIX-5B-009：5B 治理检查偏重字符串存在性，不能证明逻辑正确

**问题现状**

`check:external-call-boundary` 主要读取源码文本并检查是否出现若干关键字符串，
例如 `external_ai_disabled`、`RobotFileParser`、`synthesis_in_progress`。

`check:market-sources` 只要求就业至少存在一个直接信号，没有要求接单和产品化具备直接
信号，也没有检查来源独立性和实际内容相关性。

**影响**

- 防护字符串存在但处于错误分支时，检查仍可能通过；
- 来源目录表面覆盖三条路径即可通过，无法发现证据能力不足；
- `release-readiness` 成功容易被误解为产品逻辑已经正确。

**建议修复方向**

- 保留静态治理检查，但将关键保证转为服务级行为测试；
- 对外发关闭、逐次确认、价格变化、预算并发、模型锁、来源撤回和崩溃恢复逐项测试；
- 治理检查验证每条路径的证据能力声明与来源目录一致；
- 对来源独立性、直接信号和适用范围建立结构化契约。

**完成标准**

- 删除或移动关键保护逻辑时测试会失败；
- 仅添加一个字符串不能让检查通过；
- 市场目录无法支撑某条路径时，治理检查要求显式声明该路径不可判断。

### FIX-5B-010：E2E 只覆盖市场研究空状态，没有覆盖真实状态机

**问题现状**

当前 Playwright 流程只进入 `/market-research`，验证预算、来源名称、历史折叠区域以及
“检查官方市场来源”按钮在没有档案时禁用。

它没有覆盖：

- DeepSeek 档案创建；
- 外发总开关；
- 来源检查成功、部分失败和完全阻断；
- 外发材料预览；
- 价格失败；
- AI 综合成功和失败；
- 人工接受、拒绝；
- 来源摘录删除和综合失效；
- 后端失败后的前端状态刷新。

**影响**

“桌面与移动 E2E 通过”只能证明空页面可渲染，不能证明 5B 用户闭环可用。

**建议修复方向**

- 使用完全离线替身构建 5B 状态机 E2E；
- 桌面和移动至少覆盖一个成功流程和关键阻断流程；
- 不在 E2E 中访问真实来源或调用付费模型；
- 对加载、错误、空状态、阻断、复核和删除状态截图或断言。

**完成标准**

- 5B 主要状态转换都由 E2E 覆盖；
- 测试结束后不残留服务、端口、数据库或外部请求；
- E2E 证明的是完整离线纵向流程，而不只是空状态。

## 4. 与项目初衷或项目陈述不一致的问题

### FIX-PROJECT-001：当前诊断被称为“自适应”，实际问题顺序固定

**问题现状**

README 把当前能力写为“自适应诊断访谈，最小闭环已完成”。

但 `algorithm@0.2.0` 中每个问题的：

- `answered`；
- `skipped`；
- `uncertain`

都进入同一个下一题。当前诊断会根据部分结构化答案增加补救活动，但不会根据已有答案动态
选择下一项最有价值的问题，也不会在信息足够时提前停止。

**为什么与初衷不一致**

项目初衷要求：

- AI 根据已知信息选择下一个最有价值的问题；
- 动态、递进式访谈；
- 信息足够时停止；
- 避免固定问卷和无意义重复提问。

当前实现属于“固定问题序列 + 事后补救映射”，不能等同于已经完成自适应访谈。

**建议修复方向**

- 短期修正文档，将当前能力准确描述为“确定性诊断最小闭环”；
- 后续另行设计真正的自适应问题选择协议、停止条件和审计依据；
- 自适应实现仍必须保留跳过、不确定、修正及问题原因展示；
- 外部 AI 未启用时应有确定性安全回退，但不能伪装成动态 AI 访谈。

**完成标准**

- 当前文档不再提前宣称自适应能力已经完成；
- 真正实现后，测试证明不同答案会产生不同问题路径或提前停止；
- 每次问题选择保存可解释原因。

### FIX-PROJECT-002：cppreference 被错误标记为官方文档

**问题现状**

算法技能包把 cppreference 的 `source_type` 标记为 `official_documentation`，但
cppreference 不是 ISO C++ 标准或 C++ 标准委员会官方文档。其
`authority_rationale` 又明确写着关键标准结论仍以 ISO C++ 为准。

**影响**

- 来源类型元数据与事实不一致；
- 用户可能高估该来源的官方地位；
- 来源优先级和交叉验证逻辑可能受到错误元数据影响。

**建议修复方向**

- 使用准确的第三方参考资料类型；
- 保留其较高信誉说明，但不称为官方；
- 关键标准结论继续关联 ISO/委员会公开材料；
- 已锁定 `algorithm@0.2.0` 后不得原地修改，应按技能包不可变规则处理。

**完成标准**

- 来源类型、发布者和权威性说明互相一致；
- 修改通过新的技能包补丁版本或已确认的来源修复流程完成；
- 历史引用仍可复现。

### FIX-PROJECT-003：5B 验证记录存在不一致，完成状态需要校正

**问题现状**

当前文档中存在不同验证数量：

- `AGENTS.md` 记录 36 项后端测试、9 项前端测试；
- `TODO.md` 的后续记录写 44 项后端测试、10 项前端测试。

同时，当前 5B 全部仍是工作区未提交改动：

- 尚未提交；
- 尚未推送；
- 尚无该状态对应的远程 CI；
- 尚未执行真实来源和真实 DeepSeek 烟雾验证。

架构文档已正确说明“只能称代码实现与本地发布就绪验证完成”，但部分
`已完成` 状态容易让读者理解为真实市场闭环已经完成。

**影响**

- 后续代理无法确定哪次验证是最新事实；
- 本地历史结果可能被错误当成当前工作区结果；
- “5B 已完成”与“真实闭环尚未验证”之间存在语义冲突。

**建议修复方向**

1. 明确每次验证对应的提交或工作区摘要、时间和命令；
2. 将旧的 36/9 记录标为被 44/10 记录取代，或解释两次验证的先后关系；
3. 将“代码纵向切片完成”“本地离线验证完成”“远程 CI 完成”“真实烟雾验证完成”
   拆成独立状态；
4. 在本文件阻断问题解决前，不执行真实烟雾验证；
5. 修复后重新运行当前代码的完整验证，并记录新鲜证据。

**完成标准**

- `AGENTS.md`、`TODO.md`、README、架构文档和 Git 状态描述一致；
- 每项“已通过”都能追溯到具体提交、运行和结果；
- 未提交工作区不被描述为已经通过远程 CI；
- 真实调用未执行时继续明确标记为未验证。

## 5. 建议修复顺序

建议按以下顺序实施，避免先做界面或真实调用后再次返工：

1. 修复 `FIX-5B-001`：完整内容变化检测；
2. 修复 `FIX-5B-002`：三路径证据能力、直接信号和独立来源；
3. 修复 `FIX-5B-003`：技能、能力范围和用户目标版本锁；
4. 修复 `FIX-5B-004`：付费调用租约、崩溃恢复和保守记账；
5. 修复 `FIX-5B-005` 与 `FIX-5B-008`：访问冷却和响应模型校验；
6. 修复 `FIX-5B-006` 与 `FIX-5B-007`：知情外发、删除入口和状态刷新；
7. 加强 `FIX-5B-009` 与 `FIX-5B-010`：治理检查和完整离线 E2E；
8. 校正 `FIX-PROJECT-001` 至 `FIX-PROJECT-003` 的产品陈述和来源元数据；
9. 运行完整本地 `release-readiness`；
10. 提交并推送后验证远程 GitHub Actions；
11. 只有以上结果全部成功并由项目所有者再次确认，才执行真实付费烟雾验证。

## 6. 修复过程中不得突破的边界

- 不得因为修复 5B 而实施 5C；
- 不得实施 4B、Docker、WSL 或用户代码执行；
- 不得自动修改 `algorithm@0.2.0`；
- 不得擅自增加市场范围、模型或预算；
- 不得访问未批准来源；
- 不得真实调用 DeepSeek，除非项目所有者在修复和离线验证完成后逐次确认；
- 不得把来源首页、宏观统计或模型记忆包装成具体岗位、订单、客户预算或产品需求；
- 不得把完成修复等同于用户已经掌握算法或已经具备真实交付能力。

## 7. 本文档的关闭条件

本文件中的问题只有在以下条件同时满足时才能在 `TODO.md` 中标记为完成：

1. 对应实现已经完成；
2. 相称的后端、前端、契约、迁移、治理和 E2E 验证成功；
3. 当前工作区或提交的验证证据可复核；
4. 文档状态与 Git、代码和验证结果一致；
5. 没有通过放宽证据、隐私、费用或技能包不可变边界来规避问题；
6. 真实调用类问题仍须取得项目所有者的逐次授权。
